// Tools/Markdown/MarkdownView.swift
// STUB — placeholder so project compiles during TDD RED phase.
import SwiftUI

struct MarkdownView: View {
    let onSaveHistory: (HistoryEntry) -> Void

    var body: some View {
        Text("Markdown Previewer (stub)")
    }
}
