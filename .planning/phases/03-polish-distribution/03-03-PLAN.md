---
phase: 03-polish-distribution
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02a"]
files_modified:
  - Flint.xcodeproj/project.pbxproj
  - Info.plist
  - Core/Services/SparkleUpdaterService.swift
  - App/FlintApp.swift
  - UI/MenuBarPopoverView.swift
autonomous: false
user_setup:
  - service: sparkle-eddsa
    why: "Sparkle requires an EdDSA keypair; the private key is generated once into the login Keychain and the public key must be embedded in Info.plist from the first distributed build"
    dashboard_config:
      - task: "Run Sparkle's generate_keys once; copy the printed SUPublicEDKey value into Info.plist; back up the private key off-machine"
        location: "Sparkle 2.9.3 checkout bin/generate_keys (after SPM resolves)"
requirements: [DIST-04]
must_haves:
  truths:
    - "Sparkle is added as an SPM dependency (2.9.3) and resolves in the project"
    - "The EdDSA public key (SUPublicEDKey) is present in Info.plist in this build — BEFORE any release ships"
    - "SUFeedURL is present in Info.plist pointing at the appcast location"
    - "SparkleUpdaterService initializes SPUStandardUpdaterController lazily off the cold-start critical path (started from popover .onAppear, not FlintApp.init)"
    - "Cold start stays under 500ms (no regression from Sparkle init)"
  artifacts:
    - path: "Core/Services/SparkleUpdaterService.swift"
      provides: "@Observable @MainActor wrapper around SPUStandardUpdaterController with lazy start() and checkForUpdates()"
      contains: "SPUStandardUpdaterController"
    - path: "Info.plist"
      provides: "SUPublicEDKey and SUFeedURL keys"
      contains: "SUPublicEDKey"
  key_links:
    - from: "App/FlintApp.swift"
      to: "Core/Services/SparkleUpdaterService.swift"
      via: "@State sparkle = SparkleUpdaterService()"
      pattern: "SparkleUpdaterService"
    - from: "UI/MenuBarPopoverView.swift"
      to: "SparkleUpdaterService.start()"
      via: ".onAppear calls sparkle.start() — off cold-start path"
      pattern: "sparkle.start"
---

<objective>
Deliver the code half of DIST-04: add Sparkle 2.9.3 as an SPM dependency, embed the EdDSA public key (`SUPublicEDKey`) and `SUFeedURL` in the manual Info.plist (built in plan 03-01), and wire a lazy `SparkleUpdaterService` initialized off the cold-start critical path (RESEARCH Pitfall #6). The actual distribution/notarization/appcast dry-run is plan 03-05; this plan makes the app Sparkle-ready.

**[BLOCKING NON-NEGOTIABLE]** The EdDSA public key MUST be embedded in Info.plist in THIS build — the FIRST build that will ever be distributed. If Flint ships any release without `SUPublicEDKey`, adding it later is rejected by Sparkle as a security downgrade and locks every existing user out of auto-update permanently (RESEARCH Pitfall #5; SUMMARY Pitfall #10). The human checkpoint in this plan gates on the key being present.

Purpose: Make auto-update infrastructure correct from the first byte; a mistake here is unrecoverable in the field.
Output: Sparkle SPM dependency, SUPublicEDKey + SUFeedURL in Info.plist, SparkleUpdaterService, and lazy start wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-polish-distribution/03-CONTEXT.md
@.planning/phases/03-polish-distribution/03-RESEARCH.md
@.planning/phases/03-polish-distribution/03-PATTERNS.md
@.planning/phases/03-polish-distribution/03-01-SUMMARY.md
@.planning/phases/03-polish-distribution/03-02a-SUMMARY.md
@CLAUDE.md

<interfaces>
From Core/Services/HotkeyManager.swift (analog for the service shell):
- `@Observable @MainActor final class HotkeyManager { init() { ... } }` — the project's universal service triple.

From App/FlintApp.swift (after plan 03-01):
- `@State` service block (historyStore, prefs, clipboard, hotkeyManager, toolRegistry, toolSeed) + `@NSApplicationDelegateAdaptor(AppDelegate.self)`. Add `@State private var sparkle = SparkleUpdaterService()` alongside.

From UI/MenuBarPopoverView.swift:
- Existing `.onAppear` (line ~126) that calls `searchFocused = true` and `clipboard.start(registry: toolRegistry)`. Add `sparkle.start()` here (injected via `.environment`).
- NOTE: `MenuBarPopoverView.swift` was already modified by plan 03-02a (launcher `.fileDrop` + `dropError`/`WarningBannerView` + drag overlay) and by 03-01 (service routing `.onReceive` blocks). This plan depends_on 03-02a; read its SUMMARY before editing so your `.onAppear` and `@Environment(SparkleUpdaterService.self)` additions are layered onto the existing modifiers without clobbering them. Add `sparkle.start()` to the SAME `.onAppear` that 03-02a/03-01 left in place.

From Sparkle 2.9.3 (sparkle-project.org/documentation/programmatic-setup):
- `SPUStandardUpdaterController(startingUpdater: Bool, updaterDelegate: SPUUpdaterDelegate?, userDriverDelegate: SPUStandardUserDriverDelegate?)`.
- `.updater.checkForUpdates()` triggers a manual check.
- Sparkle does NOT check on the very first launch (intentional; do not override — RESEARCH anti-pattern).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify Sparkle package legitimacy</name>
  <action>This is a checkpoint task — the executor performs the verification described below (what-built / how-to-verify) and pauses for the human resume-signal. No code is written in this task.</action>
  <what-built>About to add Sparkle 2.9.3 as an SPM dependency. Per the package legitimacy gate, Sparkle is [ASSUMED] (slopcheck unavailable at research time) and must be human-verified before install.</what-built>
  <how-to-verify>
    1. Confirm the package source is the authoritative repo: https://github.com/sparkle-project/Sparkle (the only production-grade non-MAS macOS updater; locked in CLAUDE.md).
    2. Confirm version 2.9.3 exists on the GitHub releases page (RESEARCH verified 2026-06-08 release).
    3. Confirm CLAUDE.md "Recommended Stack" lists Sparkle 2.9.3 — this is the locked toolchain decision, not a new choice.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with adding Sparkle via SPM, or "reject" with a reason.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add Sparkle SPM dependency + SparkleUpdaterService</name>
  <read_first>
    - Flint.xcodeproj/project.pbxproj (find the XCRemoteSwiftPackageReference / packageReferences section where ChromaKit, swift-markdown, KeyboardShortcuts, GRDB are declared — copy that exact structure for Sparkle)
    - Core/Services/HotkeyManager.swift (the @Observable @MainActor final class service shell)
    - .planning/phases/03-polish-distribution/03-PATTERNS.md (section "SparkleUpdaterService.swift" — exact shell; lazy start() pattern)
    - .planning/phases/03-polish-distribution/03-RESEARCH.md (Pattern 3 + "Sparkle init (lazy, off critical path)" code; Pitfall #6)
  </read_first>
  <action>
    Add Sparkle to the Xcode project as an SPM dependency: package URL `https://github.com/sparkle-project/Sparkle`, exact version `2.9.3`. Add an `XCRemoteSwiftPackageReference` and an `XCSwiftPackageProductDependency` for product `Sparkle`, and add that product to the Flint app target's `packageProductDependencies` / frameworks build phase, mirroring how GRDB/KeyboardShortcuts/ChromaKit/swift-markdown are wired in project.pbxproj. If editing pbxproj by hand is error-prone, run `xcodebuild -resolvePackageDependencies` after adding the reference, or use the Xcode "Add Package Dependencies" UI — but the resulting Package.resolved MUST pin 2.9.3.

    Create `Core/Services/SparkleUpdaterService.swift`: `import Sparkle`, `@Observable @MainActor final class SparkleUpdaterService` with `private(set) var controller: SPUStandardUpdaterController?`. Implement `func start()` that guards `controller == nil` then creates `SPUStandardUpdaterController(startingUpdater: true, updaterDelegate: nil, userDriverDelegate: nil)` — called from the popover `.onAppear`, NOT FlintApp.init, to keep init off the cold-start critical path (Pitfall #6 / A5). Implement `func checkForUpdates() { controller?.updater.checkForUpdates() }`. Do NOT add any custom update UI — Sparkle owns the standard sheet (D-08).
  </action>
  <verify>
    <automated>cd /Users/chutipon/Documents/project/flint && grep -q "SPUStandardUpdaterController" Core/Services/SparkleUpdaterService.swift && grep -q "func start()" Core/Services/SparkleUpdaterService.swift && grep -q "startingUpdater: true" Core/Services/SparkleUpdaterService.swift && grep -qi "sparkle" Flint.xcodeproj/project.pbxproj && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - project.pbxproj references the Sparkle package (URL github.com/sparkle-project/Sparkle) and the Flint app target depends on the `Sparkle` product.
    - Package.resolved (once resolved) pins Sparkle to 2.9.3.
    - `SparkleUpdaterService` is `@Observable @MainActor final class` with a lazy `start()` (guarded on `controller == nil`) and `checkForUpdates()`.
    - No `SPUStandardUpdaterController` is constructed in `FlintApp.init` (grep FlintApp.swift shows no `SPUStandardUpdaterController`).
  </acceptance_criteria>
  <done>Sparkle 2.9.3 is a resolved SPM dependency and SparkleUpdaterService wraps the updater controller with lazy, off-critical-path initialization.</done>
</task>

<task type="auto">
  <name>Task 3: Embed SUPublicEDKey + SUFeedURL in Info.plist; wire lazy start</name>
  <read_first>
    - Info.plist (from plan 03-01 — the manual plist where keys are added)
    - App/FlintApp.swift (the @State service block + the MenuBarExtra content where environments are injected)
    - UI/MenuBarPopoverView.swift (the .onAppear at ~line 126 where clipboard.start is called)
    - .planning/phases/03-polish-distribution/03-RESEARCH.md (Pattern 3 Info.plist keys; Pitfall #5 — key from first build; "Local v0.0.1→v0.0.2 dry-run" for SUFeedURL expectations)
  </read_first>
  <action>
    Generate the EdDSA keypair: after SPM resolves Sparkle, run `generate_keys` from the Sparkle checkout's `bin/` (path under DerivedData SourcePackages/checkouts/Sparkle/bin, or build the `generate_keys` target). It prints the public key as the `SUPublicEDKey` value and stores the PRIVATE key in the login Keychain. **Do NOT write the private key to the repo, dotfiles, or env vars** (STRIDE Information Disclosure T-03-09). Record in the SUMMARY that the private key lives only in the login Keychain and must be backed up off-machine (1Password / CI secret) before any real release.

    In `Info.plist` add `SUPublicEDKey` = the printed base64 public key, and `SUFeedURL` = the appcast URL. For the v0.0.1→v0.0.2 local dry-run (plan 03-05), use a placeholder local/staging URL now (e.g. `http://localhost:8000/appcast.xml`) and note in the SUMMARY that it MUST be replaced with the production HTTPS URL before the real v1.0 release. Confirm `CFBundleVersion` (CURRENT_PROJECT_VERSION) and `CFBundleShortVersionString` (MARKETING_VERSION) are wired in the manual plist via build-setting variables so they can be bumped per release (Sparkle compares the integer CFBundleVersion: v0.0.1=1, v0.0.2=2 — RESEARCH anti-pattern).

    Wire the service: in `App/FlintApp.swift` add `@State private var sparkle = SparkleUpdaterService()` to the service block and inject via `.environment(sparkle)` on the MenuBarPopoverView content. In `UI/MenuBarPopoverView.swift` add `@Environment(SparkleUpdaterService.self) private var sparkle` and call `sparkle.start()` inside the existing `.onAppear` (alongside `clipboard.start(...)`). Optionally (builder's discretion per D-08/UI-SPEC) add a "Check for Updates…" menu item calling `sparkle.checkForUpdates()`.
  </action>
  <verify>
    <automated>cd /Users/chutipon/Documents/project/flint && plutil -lint Info.plist && /usr/libexec/PlistBuddy -c "Print :SUPublicEDKey" Info.plist | grep -qE '.{40,}' && /usr/libexec/PlistBuddy -c "Print :SUFeedURL" Info.plist | grep -q "appcast" && grep -q "sparkle.start()" UI/MenuBarPopoverView.swift && grep -q "SparkleUpdaterService()" App/FlintApp.swift && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `plutil -lint Info.plist` is OK; `SUPublicEDKey` is a non-empty base64 string (length ≥ 40) and `SUFeedURL` contains `appcast`.
    - `FlintApp.swift` declares `@State private var sparkle = SparkleUpdaterService()` and injects it via `.environment(sparkle)`.
    - `MenuBarPopoverView.swift` calls `sparkle.start()` inside `.onAppear` (off cold-start path).
    - The private key is NOT present anywhere in the repo (`grep -rl "BEGIN.*PRIVATE\|SUPrivateEDKey" .` finds nothing).
    - SUMMARY records: private key location (login Keychain), off-machine backup requirement, and that SUFeedURL is a placeholder to be replaced with production HTTPS before v1.0.
  </acceptance_criteria>
  <done>The EdDSA public key and appcast URL are embedded in Info.plist from this first build, and Sparkle starts lazily from the popover with the private key kept out of the repo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify Sparkle readiness + cold-start budget + key presence [BLOCKING]</name>
  <action>This is a checkpoint task — the executor performs the verification described below (what-built / how-to-verify) and pauses for the human resume-signal. No code is written in this task.</action>
  <what-built>Sparkle 2.9.3 SPM dependency, SUPublicEDKey + SUFeedURL in Info.plist, and a lazy SparkleUpdaterService started from the popover.</what-built>
  <how-to-verify>
    1. **[BLOCKING] Key presence:** run `/usr/libexec/PlistBuddy -c "Print :SUPublicEDKey" Info.plist` and confirm a real base64 key is printed (NOT empty, NOT a placeholder). This MUST be the actual public key from generate_keys. Confirm the private key was saved to the login Keychain (Keychain Access → search "Sparkle" / "ed25519") and that you have an off-machine backup plan. DO NOT approve if the key is missing — shipping without it is unrecoverable.
    2. Build and run Flint from Xcode. Confirm it compiles with Sparkle linked and launches normally.
    3. Cold-start check: quit Flint fully, then time-to-popover after launch should feel instant (<500ms target). If you have Instruments, run the App Launch template before/after to confirm no regression. Confirm `sparkle.start()` runs in the popover .onAppear, not at app init.
    4. Confirm no Sparkle update sheet appears on this first launch (Sparkle intentionally skips the first-launch check — correct behavior).
  </how-to-verify>
  <resume-signal>Type "approved" ONLY if SUPublicEDKey is a real key present in Info.plist and cold start is unaffected; otherwise describe the gap.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remote appcast/DMG → Flint updater | Update bundles arrive over the network; must be EdDSA-verified before install |
| Build pipeline → distributed bundle | Info.plist SUPublicEDKey must be present from first release or all future updates are rejected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | Malicious/MITM appcast or update bundle | mitigate | SUFeedURL hardcoded in Info.plist (not user-configurable, v1); every update EdDSA-signature-verified against the embedded SUPublicEDKey before install. |
| T-03-09 | Information Disclosure | EdDSA private key exposure | mitigate | generate_keys writes private key to login Keychain ONLY; grep gate asserts no private key in repo; SUMMARY mandates off-machine backup, never in repo/env. |
| T-03-10 | Tampering (permanent lockout) | SUPublicEDKey absent from first release | mitigate | [BLOCKING] checkpoint asserts the public key is present in Info.plist before any release ships (Pitfall #5). |
| T-03-11 | Spoofing | Downgrade attack via stripped signature | mitigate | Sparkle refuses appcast items whose signature doesn't match the embedded key; key never removed across versions. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Sparkle 2.9.3 is the only new package; legitimacy verified in Task 1 (blocking-human) before install; [ASSUMED] disposition from RESEARCH audit, authoritative GitHub source confirmed. |
</threat_model>

<verification>
- plutil -lint passes; SUPublicEDKey (real key) and SUFeedURL present in Info.plist.
- Sparkle 2.9.3 resolved via SPM; SparkleUpdaterService lazy-starts from popover .onAppear.
- No SPUStandardUpdaterController in FlintApp.init; no private key in repo.
- BLOCKING human checkpoint confirms key presence and cold-start budget.
</verification>

<success_criteria>
- The app is Sparkle-ready with the EdDSA public key embedded from the first build (the unrecoverable-if-missed requirement), feed URL set, and updater wired off the cold-start path. The end-to-end update flow is validated in plan 03-05.
</success_criteria>

<output>
Create `.planning/phases/03-polish-distribution/03-03-SUMMARY.md` when done.
</output>
