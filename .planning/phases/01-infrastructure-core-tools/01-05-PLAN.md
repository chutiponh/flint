---
phase: 01-infrastructure-core-tools
plan: 05
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - Tools/UUID/UUIDTransformer.swift
  - Tools/UUID/UUIDViewModel.swift
  - Tools/UUID/UUIDView.swift
  - Tools/UUID/UUIDDefinition.swift
  - LatheTests/UUIDTransformerTests.swift
autonomous: false
requirements: [UUID-01, UUID-02, UUID-03, UUID-04]

user_setup: []

must_haves:
  truths:
    - "User can generate single or bulk UUIDs (up to 1000) in v1, v4, and v5 (bulk is button-triggered)"
    - "User can parse/inspect any UUID — version, variant, embedded timestamp (v1/v7), component breakdown"
    - "User can export bulk UUIDs to clipboard (newline) or as CSV/JSON array, with uppercase/lowercase toggle and nil-UUID display"
    - "UUID v7 generation is delivered only if a sound package is vetted; otherwise it is deferred to Phase 2 with a documented stub (v7 inspection still works)"
  artifacts:
    - path: "Tools/UUID/UUIDTransformer.swift"
      provides: "Pure v4 generation, v1/v5 generation, inspect (version/variant/timestamp), export"
      contains: "enum UUIDTransformer"
    - path: "Tools/UUID/UUIDDefinition.swift"
      provides: "ToolDefinition registered in ToolRegistry"
      contains: "ToolDefinition"
  key_links:
    - from: "Core/Services/ToolRegistry.swift"
      to: "Tools/UUID/UUIDDefinition.swift"
      via: "UUIDDefinition.make() referenced by the frozen registry; stub overwritten with real Definition"
      pattern: "UUIDDefinition\\.make"
---

<objective>
Deliver the UUID tool vertical slice: v4 (native), v1/v5 (external package or hand-rolled fallback), inspection (version/variant/embedded-timestamp/component breakdown), and bulk export (clipboard/CSV/JSON, case toggle, nil display). UUID v7 generation (UUID-02) is GATED behind a time-boxed package-vetting checkpoint; if no sound package is found, v7 generation defers to Phase 2 with a documented stub while v7 INSPECTION (UUID-03) stays in scope.

Purpose: Cover the Generation category; resolve the two flagged package risks (UUID v1/v5 and v7) via explicit human-gated vetting rather than silently adding a low-trust dependency.
Output: A working UUID generator+inspector; a documented decision on v1/v5 package vs hand-roll; a documented v7 ship-or-defer outcome.
</objective>

<phase_goal>
**As a** developer, **I want to** generate and inspect UUIDs (v1/v4/v5, and v7 if available) and export them in bulk, **so that** I can produce and analyze identifiers in under a second, offline.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-infrastructure-core-tools/01-01-SUMMARY.md
@.planning/phases/01-infrastructure-core-tools/01-RESEARCH.md
@.planning/phases/01-infrastructure-core-tools/01-CONTEXT.md
@.planning/phases/01-infrastructure-core-tools/01-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Build against frozen skeleton contracts (01-01-SUMMARY.md). Reuse CopyButtonView, InlineErrorView. -->
Overwrite the `Tools/UUID/UUIDDefinition.swift` stub created by 01-01 with the real Definition (same `make()` signature); ToolRegistry already references it — no registry edit.
Foundation `UUID()` generates v4 ONLY. v1/v5 require an external package or hand-roll (pitfall #10).
nthState/UUIDV7 is FLAGGED (10 stars, no semver) — NOT to be added without the human checkpoint below.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: GATED package-vetting spike for UUID v1/v5 and v7 (time-boxed ~4h)</name>
  <read_first>
    - .planning/phases/01-infrastructure-core-tools/01-RESEARCH.md § "Package Legitimacy Audit" (nthState/UUIDV7 FLAGGED row), § "Native API Recipes" → "UUID Tool" (v1/v5 external-package note, v7 GATED note, hand-roll fallback), § "Common Pitfalls" #10 (Foundation v4-only), § "Open Questions" #1 and #2, § "Assumptions Log" A2/A3
    - CLAUDE.md § "What NOT to Use" and the UUID package risk notes
    - .planning/STATE.md § "Blockers/Concerns" (UUID v7 package choice)
  </read_first>
  <files>(no source files — package-vetting decision recorded in 01-05-SUMMARY.md; an SPM dependency is added to Lathe.xcodeproj only if approved)</files>
  <action>
    Manually vet `baarde/uuid-kit` (v1/v5) and `nthState/UUIDV7` (v7) on github.com + Swift Package Index per the how-to-verify steps below; slopcheck is unavailable in this env so verification is manual. Time-box to ~4h total. Produce two recorded decisions: (1) v1/v5 = add `baarde/uuid-kit` at an exact version OR hand-roll (CryptoKit SHA1 for v5, gettimeofday + pseudo-node-ID for v1); (2) v7 = add a vetted package OR defer UUID-02 generation to Phase 2 with a documented stub. Do NOT add any FLAGGED/[ASSUMED] package without explicit human approval — this is a blocking-human gate; auto-advance does not apply.
  </action>
  <what-built>
    Manual legitimacy vetting of: (a) `baarde/uuid-kit` for UUID v1/v5, and (b) `nthState/UUIDV7` for UUID v7. The executor inspects each package on its registry page (github.com + Swift Package Index), checks stars/age/releases/issues/maintenance, and confirms RFC-4122/9562 conformance against the bit-layout in RESEARCH.
  </what-built>
  <how-to-verify>
    1. Open https://github.com/baarde/uuid-kit and https://swiftpackageindex.com/baarde/uuid-kit — note stars, last commit, semver releases, open issues, and whether it provides `UUID.v1()` and `UUID.v5(name:namespace:)`.
    2. Open https://github.com/nthState/UUIDV7 — note it has ~10 stars and no semver releases (FLAGGED in RESEARCH).
    3. Decide, per the time-box (~4h total):
       - v1/v5: ADD `baarde/uuid-kit` if it vets as sound; OTHERWISE hand-roll v5 via CryptoKit SHA1 + RFC 4122 bit layout and v1 via gettimeofday + pseudo-node-ID (RESEARCH fallback).
       - v7: ADD only if a package vets as sound within the time-box; OTHERWISE DEFER UUID-02 generation to Phase 2 with a documented stub (v7 inspection in UUID-03 stays in scope).
    4. Record the decision (package+version added, or hand-roll, or defer) in the SUMMARY.
  </how-to-verify>
  <verify>
    <human-check>Both decisions (v1/v5 path and v7 ship-or-defer) recorded in the resume signal and the SUMMARY before Tasks 2-3 proceed; no unvetted package added without approval.</human-check>
  </verify>
  <resume-signal>Reply with the v1/v5 decision (package name+version OR "hand-roll") and the v7 decision ("add package X" OR "defer to Phase 2"). This unblocks Tasks 2-3.</resume-signal>
  <acceptance_criteria>
    - A v1/v5 decision is recorded: either `baarde/uuid-kit` (exact version) added to SPM, or the hand-roll fallback chosen.
    - A v7 decision is recorded: either a vetted package added, or UUID-02 generation explicitly deferred to Phase 2 with a stub plan.
    - No FLAGGED/[ASSUMED] package is added without this explicit human approval (security gate — auto-advance does NOT apply).
  </acceptance_criteria>
  <done>v1/v5 and v7 decisions recorded; no unvetted package added; Tasks 2-3 unblocked.</done>
</task>

<task type="auto">
  <name>Task 2: UUIDTransformer — generate (v1/v4/v5, v7 per Task 1) + inspect + export (TDD)</name>
  <read_first>
    - .planning/phases/01-infrastructure-core-tools/01-RESEARCH.md § "Native API Recipes" → "UUID Tool (UUID-01..04)" (generateV4, inspect with version/variant + v1 and v7 timestamp bit-mask extraction, export note), § "Common Pitfalls" #10, #17 (v7 timestamp extraction)
    - The Task-1 decision (which v1/v5/v7 path was chosen)
    - REQUIREMENTS.md UUID-01..04
  </read_first>
  <files>Tools/UUID/UUIDTransformer.swift, LatheTests/UUIDTransformerTests.swift</files>
  <behavior>
    - generateV4(count: 3) → 3 distinct valid v4 UUIDs; inspect each → version 4 (UUID-01, UUID-03)
    - inspect(known v1 UUID) → version 1, variant 1, non-nil embedded timestamp (UUID-03)
    - inspect(known v7 UUID) → version 7, non-nil ms-timestamp via the bit-mask (UUID-03, pitfall #17) — works regardless of whether v7 generation shipped
    - v5 generation (namespace+name) is deterministic: same inputs → same UUID (UUID-01)
    - inspect(nil UUID 00000000-...) → handled; export of a bulk set → newline / CSV / JSON forms, uppercase/lowercase toggle (UUID-04)
    - inspect of a malformed UUID string → graceful nil/error, no crash (INFRA-17)
  </behavior>
  <action>
    Create `Tools/UUID/UUIDTransformer.swift` (pure) per the RESEARCH recipe: `generateV4(count:)`, v1/v5 generation via the Task-1 path (package or hand-roll — for hand-rolled v5 use CryptoKit SHA1 + RFC 4122 bit layout), `inspect(_:)` returning version/variant/embedded-timestamp (v1 100ns-since-1582 and v7 48-bit ms per pitfall #17 bit-mask), and export helpers (newline / CSV / JSON array, case toggle, nil-UUID display). If Task 1 deferred v7 generation, add a clearly-commented `// UUID-02 v7 generation deferred to Phase 2 — see SUMMARY` stub that returns a typed "not available" result; v7 INSPECTION still works.

    Create `LatheTests/UUIDTransformerTests.swift` covering all `<behavior>` bullets, including v7 inspection of a known v7 vector and the malformed no-crash case; tests first.
  </action>
  <verify>
    <automated>xcodebuild test -scheme Lathe -destination 'platform=macOS' -only-testing:LatheTests/UUIDTransformerTests 2>&1 | tail -5 | grep -q "TEST SUCCEEDED\|** TEST"</automated>
  </verify>
  <acceptance_criteria>
    - UUIDTransformerTests pass, including v1 and v7 inspection (version/variant/timestamp) per the pitfall #17 bit-mask (UUID-03).
    - Behavior: v5 generation is deterministic for fixed namespace+name (UUID-01).
    - Behavior: if v7 generation was deferred, the stub returns a typed "not available" value (not a crash), and v7 inspection still works (UUID-02 gate honored, UUID-03 intact).
    - Source assertion: `grep -c "import SwiftUI\|import AppKit" Tools/UUID/UUIDTransformer.swift` (filtered) == 0.
  </acceptance_criteria>
  <done>UUIDTransformer generates v1/v4/v5 (and v7 if vetted), inspects any UUID including v7 timestamp, and exports in all formats — pure and tested.</done>
</task>

<task type="auto">
  <name>Task 3: UUID ViewModel + View + Definition (single/bulk generation, inspect, export)</name>
  <read_first>
    - .planning/phases/01-infrastructure-core-tools/01-CONTEXT.md D-10 (bulk generation button-triggered), D-12, D-13 (UUID is a default pin)
    - .planning/phases/01-infrastructure-core-tools/01-UI-SPEC.md § "Per-Field Copy Buttons" (per-UUID copy), § "Copywriting Contract" ("Generate"/"Generate 1000", UUID variant selector, "Count (max 1000)"), § "Detection Predicate Chain" (priority 8: UUID inspector)
    - .planning/phases/01-infrastructure-core-tools/01-01-SUMMARY.md (frozen interfaces)
    - REQUIREMENTS.md UUID-01, UUID-03, UUID-04
  </read_first>
  <files>Tools/UUID/UUIDViewModel.swift, Tools/UUID/UUIDView.swift, Tools/UUID/UUIDDefinition.swift</files>
  <action>
    Create `Tools/UUID/UUIDViewModel.swift` (`@Observable @MainActor`; single generation live, BULK generation up to 1000 BUTTON-triggered per D-10; inspect mode live-debounced 150ms; case toggle; export-format selection; `onSaveHistory` on generation/inspection). Create `Tools/UUID/UUIDView.swift` (version selector "v1 — Time-based / v4 — Random / v5 — Name-based" + v7 if available; "Count (max 1000)" field; "Generate"/"Generate 1000" buttons; per-UUID row with `CopyButtonView` (UUID-04, D-12); export buttons clipboard/CSV/JSON; uppercase/lowercase toggle; nil-UUID display; an inspect panel showing version/variant/timestamp/component breakdown — UUID-03). If v7 was deferred, hide or disable the v7 generation option with a tooltip "v7 generation coming in a later release"; keep v7 inspection active. Create `Tools/UUID/UUIDDefinition.swift` (id `uuid`, detection predicate per chain priority 8: `UUID(uuidString:) != nil`). Overwrite the `Tools/UUID/UUIDDefinition.swift` stub (created by 01-01) with the real Definition — same `make()` signature; ToolRegistry already references it, no registry edit needed.
  </action>
  <verify>
    <automated>xcodebuild -scheme Lathe -destination 'platform=macOS' build 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - Build succeeds with UUIDDefinition registered.
    - Behavior: "Generate 1000" produces 1000 rows without freezing the UI (button-triggered, D-10, UUID-01).
    - Behavior: pasting a UUID inspects it live — version/variant/embedded timestamp/component breakdown shown (UUID-03).
    - Behavior: bulk export produces newline / CSV / JSON forms with the case toggle applied; nil UUID renders (UUID-04).
    - Behavior: if v7 generation was deferred, the v7 generate option is disabled with an explanatory affordance; v7 inspection still works.
  </acceptance_criteria>
  <done>UUID tool generates (single/bulk), inspects, and exports; v7 honored per the gate decision; registered and pinned per D-13.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPM dependency → app | A potentially low-trust third-party package (UUID v1/v5/v7) could enter the build |
| user input → UUID inspect | Untrusted/malformed UUID strings cross into the inspector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering (supply chain) | UUID v1/v5/v7 package install | mitigate | [BLOCKING-HUMAN] FLAGGED `nthState/UUIDV7` (and `baarde/uuid-kit`) gated behind the Task-1 human-verify checkpoint; no [ASSUMED] package added without approval; auto-advance does NOT apply. Hand-roll fallback (CryptoKit SHA1 for v5) avoids the dependency entirely if vetting fails. |
| T-05-IV | Input Validation | UUID inspector on malformed strings | mitigate | Pure `inspect` returns nil/typed error on bad input; tested (INFRA-17). |
| T-05-ID | Information Disclosure | HistoryEntry for UUID | accept | UUIDs are non-secret generated/inspected values; standard history rows are expected and contain no credentials. |
</threat_model>

<verification>
- Task 1 human checkpoint resolved with a recorded v1/v5 and v7 decision
- `xcodebuild test -scheme Lathe -only-testing:LatheTests/UUIDTransformerTests` → pass (incl. v7 inspection)
- `xcodebuild -scheme Lathe build` → BUILD SUCCEEDED
- Behavioral: bulk generate 1000 non-blocking; inspect shows version/variant/timestamp; export forms work
</verification>

<success_criteria>
- Generate single/bulk v1/v4/v5 (and v7 if vetted), bulk button-triggered (UUID-01)
- UUID-02 (v7) shipped if a package vets sound, else deferred to Phase 2 with a stub — decision documented
- Inspect any UUID: version/variant/embedded timestamp (v1/v7)/component breakdown (UUID-03)
- Export clipboard/CSV/JSON, case toggle, nil display (UUID-04)
- No crash on malformed UUID input (INFRA-17); registered + pinned (D-13)
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-core-tools/01-05-SUMMARY.md` when done, documenting the v1/v5 package-vs-hand-roll decision AND the UUID-02 v7 ship-or-defer outcome (so STATE.md and traceability can be updated).
</output>
