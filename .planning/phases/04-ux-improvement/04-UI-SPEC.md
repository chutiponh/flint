---
phase: 4
slug: ux-improvement
status: approved
shadcn_initialized: false
preset: none
created: 2026-06-29
reviewed_at: 2026-06-29
platform: native-macos-swiftui
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for Phase 4: UX Improvement.
> Generated by gsd-ui-researcher. Consumed by gsd-planner and gsd-executor.
>
> **Platform note:** Native macOS SwiftUI application (macOS 14+). No web stack, no
> shadcn, no Tailwind. All design tokens are SwiftUI/AppKit system APIs in logical
> points (1pt = 1px at @1x; Retina renders @2x).
>
> **Continuity note:** This is a consistency pass (CONTEXT.md D-05). The design system
> established in Phases 1–3 (01-UI-SPEC.md, 02-UI-SPEC.md, 03-UI-SPEC.md) is the
> authoritative baseline. This spec adds only what Phase 4 introduces: new surfaces
> (AllToolsGridView, ToolHeaderView), new interaction patterns (⌘1–⌘9 row copy,
> arrow-key search nav, paste-back toggle), and new copy (onboarding additions, update
> status, error states for D-04/D-09). Nothing here overrides the prior contracts.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none (native SwiftUI + AppKit) | CLAUDE.md — SwiftUI + MVVM, native macOS |
| Preset | not applicable | Platform: native macOS |
| Component library | SwiftUI system + existing custom components | 01-UI-SPEC.md Component Inventory |
| Icon library | SF Symbols (system) — each `ToolDefinition` carries its own `sfSymbol` | CONTEXT.md D-01 |
| Font | System font (San Francisco) — `.system(size:weight:)` / `.system(design: .monospaced)` | REQUIREMENTS.md INFRA-15 |
| Color system | SwiftUI semantic colors (Light/Dark automatic) — no hardcoded hex | REQUIREMENTS.md INFRA-14 |

shadcn gate: **Not applicable** — project is native macOS SwiftUI, not React/Next.js/Vite.

---

## Spacing Scale

Carried forward from Phase 1 (01-UI-SPEC.md). All values in logical points, multiples of 4.

| Token | Value | SwiftUI Usage |
|-------|-------|---------------|
| xs    | 4pt   | Icon-to-label gap, badge inner padding, grid item spacing |
| sm    | 8pt   | Grid row spacing, banner vertical padding, back-header vertical inset, grid tile vertical padding, grid tile VStack internal spacing |
| md    | 16pt  | Default `.padding()` for tool content areas; grid section horizontal padding |
| lg    | 24pt  | Onboarding window padding (established `OnboardingWindowView.padding(24)`); section-level padding |
| xl    | 32pt  | Not used in 480×600 popover; workspace window major regions |
| 2xl   | 48pt  | Reserved; not used in Phase 4 new surfaces |
| 3xl   | 64pt  | Reserved; not used in Phase 4 new surfaces |

**Phase 4 exceptions / additions:**

- All-tools grid (`AllToolsGridView`): 3 adaptive columns, `GridItem(.adaptive(minimum: 100, maximum: 140), spacing: 8)`. Grid row spacing: 8pt (sm token). Grid outer padding: 12pt horizontal, 8pt vertical. 12pt is an off-scale inherited value matching the existing search bar and list row insets — do NOT normalize to 16pt this phase (would cause visual discontinuity against shipped components).
- Tool tile in the grid: `.padding(.vertical, 8)` (sm token). 8pt chosen over 12pt to keep tiles compact at 3 columns and avoid equal padding top/bottom equaling the outer horizontal inset, which would look unintentional.
- Back-affordance header (`ToolHeaderView`): 44pt minimum height (existing hit-target rule). Back button hit area: 44×44pt. Horizontal padding: 12pt (matches existing list row inset).
- Number badge on output rows (D-08): 16×16pt badge, 4pt inner padding, 4pt corner radius.
- Popover fixed size: **480×600pt** (INFRA-01 — locked, unchanged).
- Workspace window minimum: **800×600pt** (INFRA-02 — locked, unchanged).

---

## Typography

Carried forward from Phase 3 (03-UI-SPEC.md). Declared type scale: 4 sizes, 2 weights.

| Role | Size | Weight | Notes |
|------|------|--------|-------|
| Caption | 11pt | Semibold (600) | Number badge label, row index badge text — `.font(.system(size: 11, weight: .semibold, design: .monospaced))` |
| Body | 13pt | Regular (400) | Grid tool name, back-header title, onboarding refinement body text, update status messages — `.font(.system(size: 13))` |
| Subhead | 15pt | Semibold (600) | Tool tile name in grid (2-line max, centered), back-header tool name, onboarding section heads — `.font(.system(size: 15, weight: .semibold))` |
| Display | 20pt | Semibold (600) | Onboarding headline "Welcome to Flint" (existing, unchanged) — `.font(.system(size: 20, weight: .semibold))` |

**Two weights only:** Regular (400) and Semibold (600). No bold (700), no medium (500).

**Phase 4 typography notes:**

- Grid tool name: use Body (13pt regular) inside each tile for legibility at the 100–140pt column width. Do NOT use Subhead — it overruns at 3 columns. `.lineLimit(2)`, `.multilineTextAlignment(.center)`.
- Back-header tool name: use Subhead (15pt semibold). This is a heading-level element that orients the user to which tool they are in.
- Update status text (in Preferences): use Body (13pt regular) for the status message (`.foregroundStyle(.secondary)` when idle/checking), Caption (11pt) is NOT used here — it is too small for an action-confirming state.
- Number badges (D-08): Caption (11pt semibold monospaced) inside the badge shape — compact and legible at the 16pt badge size.
- Off-scale inherited exceptions (same as Phase 3, locked): 10pt sub-labels, 12pt WarningBannerView text, 22pt SF Symbol glyph size.

---

## Color

Carried forward from Phases 1–3. All SwiftUI semantic colors — no hardcoded hex.

| Role | SwiftUI Value | Usage |
|------|---------------|-------|
| Dominant (60%) | `Color(NSColor.windowBackgroundColor)` | Popover background, onboarding window, tool surfaces, AllToolsGridView container |
| Secondary (30%) | `Color(NSColor.controlBackgroundColor)` | Grid tile background (`.quaternary.opacity(0.5)` — matches RESEARCH.md Pattern 1), code display, history rows |
| Accent (10%) | `Color.accentColor` | See reserved-for list below |
| Warning | `Color.yellow.opacity(0.15)` background | WarningBannerView .warning; update "checking" spinner tint |
| Destructive / Error | `Color.red` / `Color.orange` | Error badge, destructive confirmation text |
| Text primary | `Color.primary` | All body text, tool names, heading text |
| Text secondary | `Color.secondary` | Placeholder text, timestamps, dismiss buttons, number badge foreground |
| Separator | `Color(NSColor.separatorColor)` | Zone dividers, banner borders |

**Accent (`Color.accentColor`) reserved for (explicit list — no other use):**

1. Primary CTA button background (`.borderedProminent` button style) — applies to "Open [Tool]" accept button and onboarding CTAs (existing, unchanged)
2. Tool tile SF Symbol icon foreground in `AllToolsGridView` (D-01) — the icon is the accent element; the tile background is `.quaternary.opacity(0.5)`, not accent
3. Back-button chevron icon (`ToolHeaderView`, D-02) — `chevron.left` in accent color indicates interactable nav affordance
4. Number badge focus ring (when ⌘N is pressed and a row is selected): brief `.accentColor` border flash on the copied row — 0.3s, then disappears
5. Update "Update available" label icon (`arrow.down.circle.fill` in accent) (D-03/D-04)
6. Existing uses carried from Phases 1–3: search bar focus ring, pinned history item pin icon, keyboard shortcut badge in onboarding

**Accent is NOT used for:** passive row backgrounds, section headers, secondary text, grid tile backgrounds, back-header background, update "up to date" label, update error label.

**Phase 4 new color semantics:**

- Update status colors by state:
  - `.checking`: `Color.secondary` text + `ProgressView` with system tint (accent)
  - `.upToDate`: `Color.green` label + `checkmark.circle.fill` icon
  - `.updateAvailable(version:)`: `Color.accentColor` label + `arrow.down.circle.fill` icon
  - `.error(message:)`: `Color.orange` label + `exclamationmark.triangle.fill` icon (orange, not red — this is a warning, not data loss)
- Accessibility permission reverted state in Preferences: `Color.orange` inline message ("Accessibility permission was denied…")
- Number badge background: `.quaternary.opacity(0.6)` — same muted secondary tone as tool tile background, visually subordinate to the value text

---

## Interaction Contracts

These are the behavior rules the executor must implement exactly. Each maps to a decision from CONTEXT.md and supporting detail from RESEARCH.md.

### All-Tools Grid — D-01

**New surface:** `AllToolsGridView.swift` displayed at the `.root` navigation state below the pinned tool bar and above or replacing the recent history area when the user chooses to browse.

- Layout: `LazyVGrid` with `GridItem(.adaptive(minimum: 100, maximum: 140), spacing: 8)`. 12pt outer horizontal padding, 8pt outer vertical padding.
- Tiles: `VStack(spacing: 8)` — SF Symbol icon (22pt, accent) over tool name (13pt regular, 2-line max, centered).
- Tile background: `Color.quaternary.opacity(0.5)` fill, `cornerRadius(8)`.
- Tile tap action: `navigationState = .tool(toolId: tool.id)`.
- Tile hover state: `.background(.quaternary.opacity(0.85))` on hover (standard macOS hover tint; use `.onHover` + `@State` flag).
- 12 tiles total (all registered tools). Order: `toolRegistry.tools` order (existing registry order — do not reorder).
- Grouping: flat list, no category headers this phase (Claude's discretion confirmed in CONTEXT.md).
- Keyboard navigation: mouse/tab only this phase — arrow keys NOT wired to the grid (CONTEXT.md Deferred).
- VoiceOver: each tile has `.accessibilityLabel(tool.name)` and `.accessibilityHint("Open \(tool.name)")`.
- Performance: 12 items — `LazyVGrid` instantiation is instant; no lazy loading overhead.

**Placement in `MenuBarPopoverView`:** The all-tools grid is a persistent section visible in the `.root` state, sitting below the existing 6-pinned tool bar. It replaces the current empty/sparse space below the pinned row. Recent history (last 5 entries) remains accessible by scrolling below the grid within the root body.

### Back-to-Picker Affordance — D-02

**New component:** `ToolHeaderView.swift` wrapping every tool's `tool.makeView()` call in `bodyContent`.

- Layout: a `HStack` header pinned to the top of the tool area. Left-aligned back button + centered tool name.
- Back button: `Image(systemName: "chevron.left")` in `Color.accentColor` + `Text("All Tools")` in `Color.accentColor` at 13pt regular. Rendered as a `.plain` button style. `accessibilityLabel("Back to tool picker")`.
- Tool name: `Text(tool.name)` at 15pt semibold, `Color.primary`, centered in the remaining space.
- Header height: 44pt minimum.
- Separator: `Divider()` below the header, `Color(NSColor.separatorColor)`.
- Back tap action: `navigationState = .root`. This is the same target as Esc stage-1 (which is already wired). D-02 makes it explicit and persistent — both affordances coexist.
- The header wraps ALL 12 tools uniformly. It is NOT added inside individual tool views — it wraps `tool.makeView()` at the `bodyContent` switch site in `MenuBarPopoverView`.
- VoiceOver reading order: back button first, then tool name (left-to-right).

### Arrow-Key Search Navigation — D-07

**Existing code note:** `SearchView.swift` already has `@State selectedIndex`, `.onKeyPress(.upArrow/.downArrow/.return)`, and visual highlight on `isSelected`. This contract specifies the confirmed behavior, not new code (except the TextField focus verification).

- `↑` / `↓` move `selectedIndex` through `flatResults`. Clamped: `selectedIndex` stays in `[0, flatResults.count - 1]`. Off-end presses are no-ops (no wrap-around).
- `Return` calls `activateSelected()` on the current `selectedIndex`.
- Visual highlight: row background `Color.accentColor.opacity(0.12)` when `isSelected`. Same opacity level as existing hover tints in the codebase.
- `selectedIndex` resets to `0` on every query change (`.onChange(of: query)` — already implemented).
- If `TextField` focus swallows arrow keys: escalate to `NSEvent.addLocalMonitorForEvents(matching: .keyDown)` for keyCodes 125 (↓) and 126 (↑) in the search state, consuming the event and updating `selectedIndex`. This mirrors the existing `escMonitor` pattern. Do NOT add this preemptively — test empirically first.

### ⌘1–⌘9 Row Copy — D-08

**New notification:** `Notification.Name("lathe.selectOutputRow")` with `userInfo["index"]: Int` (1–9). Added to `MenuBarPopoverView.swift` notification name extension block.

- Hidden buttons in `.background()` of `MenuBarPopoverView.body`: `KeyEquivalent(Character(String(index)))` for digits 1–9, `.command` modifier.
- Button action: posts `.selectOutputRow` notification with `index` value.
- Tool views observe: `.onReceive(NotificationCenter.default.publisher(for: .selectOutputRow))`. Handler: extract `index`, resolve the row value via `viewModel.outputForRow(index)`, write to `NSPasteboard.general`, clear prior contents first.
- Row index assignment (multi-output tools):
  - `ColorView`: row 1 = HEX, row 2 = RGB, row 3 = HSL, row 4 = HSV, row 5 = OKLCH
  - `HashView`: row 1 = MD5, row 2 = SHA-1, row 3 = SHA-256, row 4 = SHA-384, row 5 = SHA-512, row 6 = CRC32
  - `NumberBaseView`: row 1 = BIN, row 2 = OCT, row 3 = DEC, row 4 = HEX
  - All single-output tools: row 1 = their single copyable output (⌘1 = copy output, mirrors existing ⌘⇧C behavior)
- Number badge visual: 16×16pt `RoundedRectangle(cornerRadius: 4)` with `.quaternary.opacity(0.6)` fill. Badge text: digit as 11pt semibold monospaced `.foregroundStyle(.secondary)`. Badge placed at the leading edge of the row, before the format label. Tooltip (`.help("Press ⌘\(index) to copy")`).
- Copy feedback: the row's existing `CopyButtonView` shows the `"checkmark"` for 1.5s (existing behavior). No additional animation on the badge itself.
- Out-of-range index (e.g. ⌘7 when tool has only 4 rows): silently no-op. Never crash (CF-01).
- Conflict check: ⌘1–⌘9 are digit keys, NOT the letter N. `⌘N` (letter) is the existing "Open workspace window" shortcut — unchanged. No conflict.
- Accessibility: badge has `.accessibilityLabel("⌘\(index) to copy")`. Hidden buttons have `.accessibilityHidden(true)`.

### Update Checker — D-03 / D-04

**Surface:** `GeneralPreferencesTab` in `PreferencesView.swift`. A new "Updates" `Section` below existing general settings.

- Button: `Button("Check for Updates…")`. `.disabled(sparkle.updateStatus == .checking)`. Style: default (not `.borderedProminent` — this is not a primary CTA; it is a utility action). `accessibilityLabel("Check for updates")`.
- Status display below the button (appears only when `updateStatus != .idle`):
  - `.checking`: `HStack` with `ProgressView().scaleEffect(0.7)` + `Text("Checking for updates…")` at 13pt `.secondary`
  - `.upToDate`: `Label("Flint is up to date.", systemImage: "checkmark.circle.fill")` `.foregroundStyle(.green)` at 13pt
  - `.updateAvailable(version:)`: `Label("Update available: v\(version)", systemImage: "arrow.down.circle.fill")` `.foregroundStyle(.accentColor)` at 13pt
  - `.error(message:)`: `Label(message, systemImage: "exclamationmark.triangle.fill")` `.foregroundStyle(.orange)` at 13pt, `.lineLimit(3)`
- Status is cleared back to `.idle` on next popover open (do not persist update status across sessions).
- Sparkle start guard: `sparkle.checkForUpdates()` must defensively call `start()` before `controller?.updater.checkForUpdates()`. Idempotent — `start()` is already guarded by `controller == nil` check.

### Paste-Back Toggle — D-09

**Surface:** `GeneralPreferencesTab` in `PreferencesView.swift`. A new "Keyboard Flow" `Section`.

- Toggle: "Auto-paste result after copying". Default: **OFF** (`prefs.pasteBackEnabled` defaults to `false`).
- `.help("When enabled, pressing ⌘1–⌘9 copies the result AND pastes it into the previously-focused app. Requires Accessibility permission.")`.
- `accessibilityLabel("Enable automatic paste-back after copying a result")`.
- Enable flow (toggle flips to ON): `if AXIsProcessTrusted() { prefs.pasteBackEnabled = true }` else call `AXIsProcessTrustedWithOptions` with `kAXTrustedCheckOptionPrompt: true`, then poll `AXIsProcessTrusted()` every 0.5s for up to 30s, then revert toggle if not granted.
- Revert copy: clear the toggle and show an inline message: `Text("Accessibility permission was denied. To enable paste-back, go to System Settings > Privacy & Security > Accessibility and add Flint.")` + `Button("Open System Settings") { NSWorkspace.shared.open(URL(string: "x-apple.systempreferences:com.apple.preference.security?Privacy_Accessibility")!) }`.
- When toggle is ON and permission is confirmed: `Text("Accessibility permission granted. ⌘1–⌘9 will copy and paste the result into the previously-focused app.")` at 13pt `.secondary` (confirmation label below toggle).
- Paste-back action sequence (when `pasteBackEnabled && AXIsProcessTrusted()`): (1) write to `NSPasteboard.general`, (2) set `clipboard.isPopoverPresented = false`, (3) `app.activate(options: [.activateIgnoringOtherApps])`, (4) `DispatchQueue.main.asyncAfter(deadline: .now() + 0.08)` → synthesize `CGEvent` ⌘V.
- Previously-focused app capture: in `HotkeyManager`, capture `NSWorkspace.shared.frontmostApplication` BEFORE posting `.showPopover` notification. Store as `var previousFrontmostApp: NSRunningApplication?` on `HotkeyManager`.
- At paste time: re-verify `AXIsProcessTrusted()` — if permission was revoked since toggle-on, fall back to copy-only silently (do not crash, do not paste).

### Visual Consistency Pass — D-05

This applies across all 12 tool views and the launcher. Normalized values:

- All tool content area padding: `md` (16pt). Any tool using 12pt or 10pt for content-area padding should be aligned to 16pt — unless the element is a row inset that matches existing shipped components (where 12pt is locked).
- Section dividers within tools: `Divider()` with `Color(NSColor.separatorColor)` — no custom colors.
- Empty/loading states: every tool must show a non-empty state when the input field is blank. Copy: "Paste or type content above" at 13pt `.secondary`, centered. No blank white space.
- Light/Dark: confirm all tool views use only semantic colors. Any `Color(hex:)` or hardcoded `.white` / `.black` literals must be replaced with `Color.primary` / `Color(NSColor.windowBackgroundColor)` respectively.
- Loading state for file operations (Hash file, Base64 file): always shows `ProgressView()` with a descriptive label — never shows a blank area.

### Onboarding Refinement — D-06

**Extends existing `OnboardingWindowView.swift`** (do not rebuild). Add two new capability callout steps after the existing Step 2 (global hotkey), keeping the same `HStack(icon + VStack(heading + body))` layout pattern.

- Step 3 (Services menu):
  - Icon: `Image(systemName: "text.cursor")` at 22pt, accent color
  - Heading: "Route text from any app" at 15pt semibold
  - Body: "Select text anywhere, right-click, and choose Services > Open in Flint to process it instantly." at 13pt `.secondary`
- Step 4 (Drag-and-drop):
  - Icon: `Image(systemName: "arrow.down.circle")` at 22pt, accent color
  - Heading: "Drag files directly" at 15pt semibold
  - Body: "Drop a text or binary file onto any tool — Base64 and Hash accept any file type." at 13pt `.secondary`
- Note on clipboard auto-detect: the existing detection banner teaches this at runtime. No new onboarding step needed for clipboard auto-detect.
- Window size: extend height from 360pt to **480pt** to accommodate two new steps without scroll. Width: 480pt (unchanged).
- Primary CTA and Skip copy: **unchanged** from Phase 3. The CTAs are already correct.
- VoiceOver: all new `Image` icons are `.accessibilityHidden(true)`. New `Text` elements are readable in logical order.

---

## Copywriting Contract

### New Elements — Phase 4

| Element | Copy |
|---------|------|
| All-tools grid section title (optional sub-label above grid) | "All Tools" — 13pt semibold, `.secondary` foreground |
| Grid tile accessibility label | `tool.name` |
| Grid tile accessibility hint | "Open \(tool.name)" |
| Back-affordance button label | "All Tools" (matches the grid section name for orientation coherence) |
| Back-affordance accessibility label | "Back to tool picker" |
| Number badge tooltip (each output row) | "Press ⌘\(index) to copy" |
| Number badge accessibility label | "⌘\(index) to copy" |
| Update: checking state | "Checking for updates…" |
| Update: up to date | "Flint is up to date." |
| Update: update available | "Update available: v\(version)" |
| Update: error | `error.localizedDescription` (pass-through — no custom wrapping; ensures "The connection was refused." / "A server with the specified hostname could not be found." appear verbatim) |
| Update button label | "Check for Updates…" (ellipsis indicates an in-progress action — macOS HIG convention) |
| Paste-back toggle label | "Auto-paste result after copying" |
| Paste-back toggle description | "⌘1–⌘9 copies the result AND pastes it into the previously-focused app." |
| Paste-back permission prompt (inline, when denied) | "Accessibility permission was denied. To enable paste-back, go to System Settings > Privacy & Security > Accessibility and add Flint." |
| Paste-back system settings button | "Open System Settings" |
| Paste-back permission confirmed | "Accessibility permission granted. ⌘1–⌘9 will copy and paste the result into the previously-focused app." |
| Empty state — any tool with blank input | "Paste or type content above" |
| Onboarding Step 3 heading | "Route text from any app" |
| Onboarding Step 3 body | "Select text anywhere, right-click, and choose Services > Open in Flint to process it instantly." |
| Onboarding Step 4 heading | "Drag files directly" |
| Onboarding Step 4 body | "Drop a text or binary file onto any tool — Base64 and Hash accept any file type." |

### Carried Forward — Unchanged

All copy from Phase 1 (01-UI-SPEC.md § Copywriting Contract) and Phase 3 (03-UI-SPEC.md § Copywriting Contract) carries forward without change. This includes: all tool-specific error strings, JWT warning banners, detection banner format, history panel labels, search placeholder, and all onboarding CTAs.

### Destructive Actions

No new destructive actions in Phase 4. Existing destructive actions (history clear/delete) are unchanged from Phase 1.

---

## Component Inventory

### New Components — Phase 4

| Component | File | Description |
|-----------|------|-------------|
| `AllToolsGridView` | `UI/AllToolsGridView.swift` | 3-column adaptive grid of all 12 tools. Props: `toolRegistry: ToolRegistry`, `onSelect: (String) -> Void`. Stateless — navigation state lives in `MenuBarPopoverView`. |
| `ToolHeaderView` | `UI/ToolHeaderView.swift` | Back-affordance header wrapper. Props: `toolName: String`, `onBack: () -> Void`. Renders chevron.left + "All Tools" button + centered tool name + `Divider()`. |
| `OutputRowBadge` | `UI/Components/OutputRowBadge.swift` | Numbered copy badge for multi-output rows. Props: `index: Int`. Renders 16×16pt badge with digit. No actions — ⌘N is wired at the popover level. |
| `PasteBackService` | `Core/Services/PasteBackService.swift` | Isolates CGEvent paste logic (D-09). Methods: `synthesizePaste(into: NSRunningApplication)`. Guards `AXIsProcessTrusted()` at call time. |

### Extended Components — Phase 4

| Component | File | Phase 4 Changes |
|-----------|------|-----------------|
| `MenuBarPopoverView` | `UI/MenuBarPopoverView.swift` | Add `AllToolsGridView` to `.root` state below pinned bar; wrap `tool.makeView()` with `ToolHeaderView` in `.tool` state; add hidden ⌘1–⌘9 buttons posting `.selectOutputRow`; add `.selectOutputRow` notification name |
| `PreferencesView` (GeneralPreferencesTab) | `UI/PreferencesView.swift` | Add "Updates" section (D-03 button + status display); add "Keyboard Flow" section (D-09 paste-back toggle + permission flow) |
| `OnboardingWindowView` | `UI/OnboardingWindowView.swift` | Add Steps 3 and 4 (Services, drag-drop); extend window height from 360pt to 480pt |
| `SparkleUpdaterService` | `Core/Services/SparkleUpdaterService.swift` | Adopt `SPUUpdaterDelegate`; add `@Observable var updateStatus: UpdateStatus`; change `updaterDelegate: nil` to `updaterDelegate: self`; add `start()` defensive call in `checkForUpdates()` |
| `PreferencesStore` | `Core/Services/PreferencesStore.swift` | Add `var pasteBackEnabled: Bool` (default `false`) |
| `HotkeyManager` | `Core/Services/HotkeyManager.swift` | Capture `NSWorkspace.shared.frontmostApplication` before posting `.showPopover`; expose `previousFrontmostApp: NSRunningApplication?` |
| Multi-output tool views (ColorView, HashView, NumberBaseView) | `Tools/*/` | Add `OutputRowBadge(index: N)` to each copyable row; add `.selectOutputRow` observer implementing `outputForRow(index:)` |
| All 12 tool views | `Tools/*/` | D-05 consistency pass: verify semantic color usage, 16pt content padding, empty state copy, Light/Dark compliance |
| `SearchView` | `UI/SearchView.swift` | Verify ↑↓ arrow key handling with `TextField` focus; if blocked, add `NSEvent.addLocalMonitorForEvents` for keyCodes 125/126 |

### Reused Components — No Change

`CopyButtonView`, `DetectionBannerView`, `SyntaxEditorView`, `CodeDisplayView`, `InlineErrorView`, `HistoryRowView`, `PinnedToolBarView`, `WarningBannerView`, `ProgressHashView`, `DropOverlayView`, `BitFieldView`, `ToolShortcutActions`, `WebPreviewView`.

---

## Accessibility Contract

Carried from Phase 1 (01-UI-SPEC.md) and Phase 3 (03-UI-SPEC.md). Phase 4 additions:

- `AllToolsGridView` tiles: `.accessibilityLabel(tool.name)` + `.accessibilityHint("Open \(tool.name)")` on each tile `Button`.
- `ToolHeaderView` back button: `.accessibilityLabel("Back to tool picker")`. Tool name text: `.accessibilityAddTraits(.isHeader)`.
- `OutputRowBadge`: `.accessibilityLabel("⌘\(index) to copy")`. Hidden shortcut buttons in popover background: `.accessibilityHidden(true)`.
- Paste-back toggle: `.accessibilityLabel("Enable automatic paste-back after copying a result")`.
- Update status labels: read as part of the Preferences `Section` — no additional `.accessibilityElement` grouping needed (macOS Form handles this automatically).
- Onboarding new steps: icon `Image` elements are `.accessibilityHidden(true)`; all `Text` elements readable in logical top-to-bottom order.
- All new `Button` elements without visible labels must have explicit `.accessibilityLabel` strings.
- Dynamic Type: all new text views use `.font(.system(size:weight:))` — no fixed-size containers that would clip text.

---

## Registry Safety

Not applicable. Native macOS SwiftUI application. No shadcn, no web component registries. No third-party component vetting required.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — SwiftUI native app |
| Third-party web registries | none | not applicable |

**New Swift packages this phase: none.** All Phase 4 capabilities use system frameworks (ApplicationServices, CoreGraphics, AppKit) or existing packages (Sparkle 2.9.3, KeyboardShortcuts 3.0.1, GRDB 7.11.1). Package Legitimacy Audit skipped — no new external packages.

---

## Pre-Population Sources

| Section | Source | Decision Count |
|---------|--------|----------------|
| Design System | CLAUDE.md, RESEARCH.md Standard Stack | 5 decisions |
| Spacing | 01-UI-SPEC.md + RESEARCH.md Pattern 1 (grid values) | 8 decisions |
| Typography | 03-UI-SPEC.md (confirmed 4-size scale) | 6 decisions |
| Color | 01-UI-SPEC.md + 03-UI-SPEC.md (semantic system) | 7 decisions |
| Interaction — grid | CONTEXT.md D-01 + RESEARCH.md Pattern 1 | 6 decisions |
| Interaction — back nav | CONTEXT.md D-02 + RESEARCH.md §Architecture | 5 decisions |
| Interaction — arrow nav | CONTEXT.md D-07 + RESEARCH.md Pattern 4 (current state) | 4 decisions |
| Interaction — ⌘N row copy | CONTEXT.md D-08 + RESEARCH.md Pattern 2 | 6 decisions |
| Interaction — paste-back | CONTEXT.md D-09 + RESEARCH.md OQ-01 (confirmed: Accessibility required) | 8 decisions |
| Interaction — update checker | CONTEXT.md D-03/D-04 + RESEARCH.md Pattern 3 | 5 decisions |
| Onboarding | CONTEXT.md D-06 + existing OnboardingWindowView.swift | 4 decisions |
| Copywriting | CONTEXT.md + RESEARCH.md code examples | 15 decisions |
| Component inventory | RESEARCH.md § Recommended File Changes | 8 decisions |
| Accessibility | 01-UI-SPEC.md + INFRA-15 | carried |
| Registry | RESEARCH.md (no new packages) | 1 decision |
| User input (this session) | 0 — all fields pre-populated from upstream artifacts | 0 |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
