---
phase: 04-ux-improvement
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - Tools/JSONFormatter/JSONFormatterView.swift
  - Tools/Base64/Base64View.swift
  - Tools/URLEncoder/URLView.swift
  - Tools/JWT/JWTView.swift
  - Tools/Timestamp/TimestampView.swift
  - Tools/Hash/HashView.swift
  - Tools/UUID/UUIDView.swift
  - Tools/Regex/RegexView.swift
  - Tools/Color/ColorView.swift
  - Tools/Markdown/MarkdownView.swift
  - Tools/NumberBase/NumberBaseView.swift
  - Tools/TextDiff/TextDiffView.swift
  - UI/OnboardingWindowView.swift
autonomous: true
requirements: [UX-07, UX-08, INFRA-14, INFRA-15]
decisions: [D-05, D-06, CF-01, CF-03]  # D-05 visual consistency pass, D-06 onboarding Steps 3/4 (refine existing per CF-03)
must_haves:
  truths:
    - "D-05: All 12 tool views use only SwiftUI semantic colors with no hardcoded hex / .white / .black literals (no Light/Dark artifacts)"
    - "Every tool shows a non-empty state ('Paste or type content above') when its input is blank — no blank white space"
    - "Tool content-area padding, dividers, and loading states are consistent across the 12 tools per the UI-SPEC consistency pass"
    - "D-06: First-run onboarding surfaces the Services menu and drag-and-drop capabilities (Steps 3 and 4)"
  artifacts:
    - path: "UI/OnboardingWindowView.swift"
      provides: "Onboarding Steps 3 (Services) and 4 (drag-drop); window height 480pt"
      contains: "Route text from any app"
  key_links:
    - from: "tool views"
      to: "semantic colors + empty-state copy"
      via: "D-05 consistency normalization"
      pattern: "Paste or type content above"
---

<objective>
Perform the visual consistency pass (CONTEXT.md D-05) across all 12 tool views and refine first-run onboarding (D-06). D-05 is a consistency pass, not a redesign: normalize spacing, dividers, empty/loading states, and confirm Light/Dark semantic-color compliance against the Phase 1–3 baseline. D-06 adds two capability-callout steps (Services menu, drag-and-drop) to the existing `OnboardingWindowView` without rebuilding it.

Purpose: Phase 4 success criteria 3 ("The 12 tools and launcher share a consistent, intentional visual treatment … with no Light/Dark artifacts") and 4 ("A new user … is made aware of the hotkey, Services, drag-and-drop, and clipboard auto-detect features"). Requirements UX-07 (onboarding), UX-08 (visual consistency), INFRA-14 (Light/Dark, no chrome), INFRA-15 (VoiceOver).
Output: 12 tool views normalized to the D-05 contract; `OnboardingWindowView.swift` with Steps 3 and 4 and a 480pt window height.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ux-improvement/04-CONTEXT.md
@.planning/phases/04-ux-improvement/04-UI-SPEC.md
@.planning/phases/04-ux-improvement/04-PATTERNS.md

<interfaces>
<!-- From PATTERNS.md + UI-SPEC. No exploration required. -->

OnboardingWindowView.swift existing step pattern (lines 39-75) + frame (line 116):
  HStack(alignment: .top, spacing: 12) { Image(systemName:...).font(.system(size:22)).foregroundColor(.accentColor).frame(width:28).accessibilityHidden(true)
    VStack(alignment:.leading, spacing:4) { Text(heading).font(.system(size:15, weight:.semibold)); Text(body).font(.system(size:13)).foregroundColor(.secondary).fixedSize(horizontal:false, vertical:true) } }
  .frame(width: 480, height: 360)  // CHANGE height -> 480

D-05 normalized values (UI-SPEC § Visual Consistency Pass):
  - tool content-area padding: md (16pt) — but row insets matching shipped components stay 12pt (locked)
  - dividers: Divider() with Color(NSColor.separatorColor), no custom colors
  - empty state: "Paste or type content above" 13pt .secondary centered when input blank
  - Light/Dark: only semantic colors; replace any Color(hex:)/.white/.black with Color.primary / Color(NSColor.windowBackgroundColor)
  - file ops (Hash file, Base64 file): always show ProgressView() with label, never blank

fileDrop + DropOverlayView pattern (PATTERNS.md Shared Patterns) — apply to any tool view missing it during the pass.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-05 consistency pass across all 12 tool views</name>
  <files>Tools/JSONFormatter/JSONFormatterView.swift, Tools/Base64/Base64View.swift, Tools/URLEncoder/URLView.swift, Tools/JWT/JWTView.swift, Tools/Timestamp/TimestampView.swift, Tools/Hash/HashView.swift, Tools/UUID/UUIDView.swift, Tools/Regex/RegexView.swift, Tools/Color/ColorView.swift, Tools/Markdown/MarkdownView.swift, Tools/NumberBase/NumberBaseView.swift, Tools/TextDiff/TextDiffView.swift</files>
  <read_first>
    - 04-UI-SPEC.md § "Visual Consistency Pass — D-05" (the authoritative normalized values: padding, dividers, empty-state copy, Light/Dark, loading states)
    - 04-UI-SPEC.md § Spacing Scale + § Color + § Typography (the locked token system this pass reconciles TO — do not invent new tokens)
    - 04-PATTERNS.md § Shared Patterns ("fileDrop + DropOverlayView pattern", semantic color patterns) — apply only where a view is missing the established pattern
    - Each tool view being modified (read the specific view before editing it; do not edit ViewModels in this task)
    - .planning/phases/01-infrastructure-core-tools/01-UI-SPEC.md and 03-UI-SPEC.md (the baseline the consistency pass reconciles to — do NOT introduce a new visual system, D-05)
  </read_first>
  <action>
Audit and normalize each of the 12 tool views to the D-05 contract. This is a consistency pass, NOT a redesign — reconcile to the existing Phase 1–3 baseline, do not invent a new system. For each view:

1. Replace any hardcoded color literal (`Color(hex:)`, `Color(red:green:blue:)` used as chrome, `.white`, `.black`) with the correct semantic color: text -> `Color.primary`/`.secondary`, surfaces -> `Color(NSColor.windowBackgroundColor)`/`Color(NSColor.controlBackgroundColor)`, separators -> `Color(NSColor.separatorColor)` (INFRA-14). NOTE: legitimate domain-logic color literals are NOT chrome and must be left unchanged — e.g. `ColorViewModel`'s user-RGBA-to-SwiftUI.Color for the system ColorPicker, and any swatch that renders the user's actual color value (per STATE.md Phase 03 decision). Only chrome/layout literals are normalized.
2. Normalize tool content-area `.padding()` to `md` (16pt) where a view uses 12pt or 10pt for the CONTENT AREA. Do NOT change row insets that match shipped components (12pt row insets are locked per UI-SPEC Spacing § exceptions) — changing those causes visual discontinuity.
3. Ensure every section divider uses a plain `Divider()` (separator color), no custom-colored rectangles.
4. Ensure every tool shows a non-empty state when its primary input is blank: a centered `Text("Paste or type content above")` at `.font(.system(size: 13))` `.foregroundStyle(.secondary)` — no blank white space (UI-SPEC empty-state copy, verbatim).
5. For file-processing tools (Hash file, Base64 file), confirm the in-progress UI always shows a `ProgressView()` with a descriptive label, never a blank area.

Do not alter tool logic, ViewModels, or the D-08 OutputRowBadge rows added in Plan 03 (Color/Hash/NumberBase) — preserve them. Touch only chrome/layout/empty-state. Keep CF-01: no change may regress cold-start or freeze on input.
  </action>
  <verify>
    <automated>! grep -rEn "Color\.white|Color\.black|\.foregroundColor\(\.white\)|\.foregroundColor\(\.black\)" Tools/*/*.swift | grep -v "//" ; grep -rl "Paste or type content above" Tools/ | wc -l | grep -qE "[1-9]" && grep -q "Paste or type content above" Tools/JSONFormatter/JSONFormatterView.swift</automated>
  </verify>
  <acceptance_criteria>
    - No tool view contains chrome color literals `.white` / `.black` / `Color(hex:)` (grep, excluding comments and legitimate domain-logic color values like the ColorPicker swatch) — INFRA-14.
    - Every tool that has a primary text input shows the verbatim empty-state copy "Paste or type content above" at 13pt `.secondary` when blank (UI-SPEC empty-state).
    - Section dividers use plain `Divider()` (no custom-colored separators).
    - File-processing tools show `ProgressView()` with a label during file ops (no blank area).
    - Plan 03's `OutputRowBadge` rows in Color/Hash/NumberBase are preserved (grep still finds `OutputRowBadge(index:` in all three).
    - The whole app BUILDS.
  </acceptance_criteria>
  <done>All 12 tool views use semantic colors only, show a non-empty state on blank input, use consistent dividers and loading states, and retain Plan 03's badge rows; the app builds.</done>
</task>

<task type="auto">
  <name>Task 2: D-06 onboarding Steps 3 (Services) and 4 (drag-drop)</name>
  <files>UI/OnboardingWindowView.swift</files>
  <read_first>
    - UI/OnboardingWindowView.swift (the file being modified — existing Step 1/2 HStack pattern lines 39-75, frame line 116, Spacer(minLength: 0) insertion point)
    - 04-PATTERNS.md § "UI/OnboardingWindowView.swift — D-06 Steps 3 and 4" (exact Step 3/4 HStack excerpts + height change)
    - 04-UI-SPEC.md § "Onboarding Refinement — D-06" + § Copywriting Contract (verbatim Step 3/4 headings and bodies)
    - .planning/phases/03-polish-distribution/03-CONTEXT.md (D-07 onboarding origin — refine, do not rebuild)
  </read_first>
  <action>
Extend the existing `OnboardingWindowView.swift` (do not rebuild — D-06). After the existing Step 2 (global hotkey) and before `Spacer(minLength: 0)`, add two new capability callout steps using the SAME `HStack(alignment: .top, spacing: 12) { Image + VStack(heading + body) }` pattern as Steps 1–2:

Step 3 (Services menu): `Image(systemName: "text.cursor")` at 22pt accent, `.accessibilityHidden(true)`; heading `Text("Route text from any app")` at 15pt semibold; body `Text("Select text anywhere, right-click, and choose Services > Open in Flint to process it instantly.")` at 13pt `.secondary` with `.fixedSize(horizontal: false, vertical: true)`.

Step 4 (drag-and-drop): `Image(systemName: "arrow.down.circle")` at 22pt accent, `.accessibilityHidden(true)`; heading `Text("Drag files directly")` at 15pt semibold; body `Text("Drop a text or binary file onto any tool — Base64 and Hash accept any file type.")` at 13pt `.secondary` with `.fixedSize(horizontal: false, vertical: true)`.

Use the exact copy from the UI-SPEC Copywriting Contract. Do NOT add a separate clipboard-auto-detect step — the existing detection banner teaches that at runtime (UI-SPEC D-06 note). Change `.frame(width: 480, height: 360)` to `.frame(width: 480, height: 480)` to fit the two new steps without scroll. Leave the existing headline, primary CTA, and Skip copy unchanged (UI-SPEC: CTAs already correct). New icon `Image`s are `.accessibilityHidden(true)`; new `Text` is readable in logical order (INFRA-15).
  </action>
  <verify>
    <automated>grep -q '"Route text from any app"' UI/OnboardingWindowView.swift && grep -q '"Drag files directly"' UI/OnboardingWindowView.swift && grep -q "text.cursor" UI/OnboardingWindowView.swift && grep -q "arrow.down.circle" UI/OnboardingWindowView.swift && grep -q "height: 480" UI/OnboardingWindowView.swift && xcodebuild -scheme Flint -configuration Debug -destination 'platform=macOS' build CODE_SIGNING_ALLOWED=NO -quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `OnboardingWindowView.swift` contains Step 3 with `text.cursor` icon and heading "Route text from any app" + the verbatim Services body (UI-SPEC Copywriting Contract).
    - Step 4 with `arrow.down.circle` icon and heading "Drag files directly" + the verbatim drag-drop body.
    - Window frame height changed from 360 to 480 (UI-SPEC § Onboarding Refinement).
    - New icons are `.accessibilityHidden(true)`; existing headline/CTA/Skip copy unchanged.
    - The whole app BUILDS.
  </acceptance_criteria>
  <done>First-run onboarding now surfaces the Services menu and drag-and-drop capabilities as two new steps matching the existing visual pattern, at a 480pt window height; the app builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This plan is presentation-only: color/spacing normalization and static onboarding copy. No new untrusted input, no new external surface, no permissions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | Accidental removal of legitimate domain color values during the consistency pass | mitigate | Only chrome/layout color literals are normalized; legitimate domain-logic color values (ColorPicker swatch, user-RGBA rendering — STATE.md Phase 03 decision) are explicitly preserved. Build + visual check confirm no functional color regression. |
| T-04-10 | Information disclosure | Onboarding copy leaking internal/sensitive detail | accept | Onboarding copy is fixed marketing/help text from the UI-SPEC Copywriting Contract; no user data or secrets surfaced. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs — no new SPM packages this phase (RESEARCH.md). Package Legitimacy Audit not applicable. |
</threat_model>

<verification>
- `xcodebuild -scheme Flint build` succeeds.
- No chrome `.white`/`.black`/`Color(hex:)` literals remain in tool views (INFRA-14); domain color values preserved.
- Every tool shows "Paste or type content above" on blank input.
- Onboarding shows 4 capability steps at 480pt height with the verbatim Step 3/4 copy.
- Plan 03's badge rows intact in Color/Hash/NumberBase.
</verification>

<success_criteria>
- All 12 tools share consistent spacing, dividers, empty/loading states, and semantic-color compliance (D-05, UX-08, INFRA-14).
- Onboarding surfaces Services + drag-and-drop (D-06, UX-07).
- App builds; no logic or CF-01 regression.
</success_criteria>

<output>
Create `.planning/phases/04-ux-improvement/04-04-SUMMARY.md` when done.
</output>
