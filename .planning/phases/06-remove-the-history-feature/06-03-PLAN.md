---
phase: 06-remove-the-history-feature
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Tools/UUID/UUIDViewModel.swift
  - Tools/UUID/UUIDView.swift
  - Tools/UUID/UUIDDefinition.swift
  - Tools/Timestamp/TimestampViewModel.swift
  - Tools/Timestamp/TimestampView.swift
  - Tools/Timestamp/TimestampDefinition.swift
  - Tools/TextDiff/TextDiffViewModel.swift
  - Tools/TextDiff/TextDiffView.swift
  - Tools/TextDiff/TextDiffDefinition.swift
  - Tools/Markdown/MarkdownViewModel.swift
  - Tools/Markdown/MarkdownView.swift
  - Tools/Markdown/MarkdownDefinition.swift
  - Tools/ImageCompress/ImageCompressViewModel.swift
  - Tools/ImageCompress/ImageCompressView.swift
  - Tools/ImageCompress/ImageCompressDefinition.swift
  - FlintTests/ImageCompressViewModelTests.swift
autonomous: true
requirements: [INFRA-08]
must_haves:
  truths:
    - "UUID, Timestamp, Text Diff, Markdown, and Image Compress tools compile with no history capture"
    - "ImageCompressViewModelTests no longer references onSaveHistory and the history-fires-once test is removed"
  artifacts:
    - path: "Tools/UUID/UUIDDefinition.swift"
      provides: "UUID Definition with no HistoryStore wrapper"
    - path: "Tools/ImageCompress/ImageCompressViewModel.swift"
      provides: "Image compress VM with no onSaveHistory"
    - path: "FlintTests/ImageCompressViewModelTests.swift"
      provides: "Test file constructing ImageCompressViewModel() with no closure, no history test"
  key_links:
    - from: "Tools/UUID/UUIDDefinition.swift"
      to: "UUIDView()"
      via: "makeView builds view with no closure"
      pattern: "UUIDView\\(\\)"
---

<objective>
Strip all per-tool history capture from the UUID, Timestamp, Text Diff, Markdown, and Image Compress tools (all Wrapper-pattern), and clean the ImageCompressViewModelTests test file that constructs `ImageCompressViewModel(onSaveHistory:)` in 11 places and has a dedicated "history fires once" test.

Purpose: These five tools plus their one test file stop referencing the history subsystem so it can be deleted in Wave 3. The test-file cleanup lives here (not in the Wave-3 file-deletion plan) because the test target must keep compiling — `ImageCompressViewModel(onSaveHistory:)` call sites break the moment the VM's init changes, so VM change and test change must ship together.
Output: Five tools and one test file that build with zero history references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

<pattern_note>
All five tools use the Wrapper-pattern: a `...ViewWrapper` (or inline wrapper) in the Definition file reads `@Environment(HistoryStore.self)` and passes a closure to the View's `init(onSaveHistory:)`, which forwards to `SomeViewModel(onSaveHistory:)`.
Markdown differs slightly: MarkdownView has a `let onSaveHistory` property (line 13) and builds the VM at line 28 — same removal, just a stored `let` instead of a custom init.
</pattern_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove history from UUID and Timestamp tools</name>
  <read_first>
    Tools/UUID/UUIDViewModel.swift (onSaveHistory at 53/56, calls at 102 and 144),
    Tools/UUID/UUIDView.swift (init(onSaveHistory:) at 17 → VM at 18),
    Tools/UUID/UUIDDefinition.swift (wrapper with @Environment(HistoryStore.self), 2 history refs),
    Tools/Timestamp/TimestampViewModel.swift (onSaveHistory at 72/75, call at 184),
    Tools/Timestamp/TimestampView.swift (init(onSaveHistory:) at 11 → VM at 12),
    Tools/Timestamp/TimestampDefinition.swift (wrapper with @Environment(HistoryStore.self))
  </read_first>
  <action>
    For BOTH UUID and Timestamp (Wrapper pattern):
    In each ViewModel: delete the `onSaveHistory` stored property, the init param, and EVERY `onSaveHistory(HistoryEntry(...))` call (UUID has two: ~102 and ~144; Timestamp one at ~184). Keep generation/parse/convert logic unchanged.
    In each View: change `init(onSaveHistory:)` to a no-closure init building `SomeViewModel()`.
    In each Definition: delete the ViewWrapper struct and its `@Environment(HistoryStore.self)`; change `makeView` to `AnyView(UUIDView())` / `AnyView(TimestampView())` directly.
  </action>
  <acceptance_criteria>
    `grep -rn "onSaveHistory\|HistoryEntry\|HistoryStore" Tools/UUID/ Tools/Timestamp/` returns nothing.
    `grep -n "UUIDView()" Tools/UUID/UUIDDefinition.swift` and `grep -n "TimestampView()" Tools/Timestamp/TimestampDefinition.swift` both match.
  </acceptance_criteria>
  <done>UUID and Timestamp tools build with zero history references.</done>
</task>

<task type="auto">
  <name>Task 2: Remove history from Text Diff and Markdown tools</name>
  <read_first>
    Tools/TextDiff/TextDiffViewModel.swift (onSaveHistory at 50/68, call at 106),
    Tools/TextDiff/TextDiffView.swift (@Environment/init; preview .environment(HistoryStore()) at 546),
    Tools/TextDiff/TextDiffDefinition.swift (wrapper with @Environment(HistoryStore.self)),
    Tools/Markdown/MarkdownViewModel.swift (onSaveHistory at 36/41, call at 86),
    Tools/Markdown/MarkdownView.swift (let onSaveHistory at 13, VM build at 28),
    Tools/Markdown/MarkdownDefinition.swift (wrapper with @Environment(HistoryStore.self)),
    Tools/UUID/UUIDDefinition.swift as the just-cleaned Wrapper reference (read after Task 1)
  </read_first>
  <action>
    For BOTH Text Diff and Markdown:
    In each ViewModel: delete the `onSaveHistory` stored property, init param, and the `onSaveHistory(HistoryEntry(...))` call. Keep diff computation / markdown render logic unchanged.
    In TextDiffView.swift: remove the `onSaveHistory` init param (build VM with no closure); delete the `.environment(HistoryStore())` line in the `#Preview` (~546).
    In MarkdownView.swift: delete the `let onSaveHistory: (HistoryEntry) -> Void` property (line 13) and the `onSaveHistory:` argument in the `MarkdownViewModel(onSaveHistory:)` construction (line 28) so it becomes `MarkdownViewModel()`.
    In each Definition: delete the ViewWrapper struct and its `@Environment(HistoryStore.self)`; change `makeView` to `AnyView(TextDiffView())` / `AnyView(MarkdownView())` directly.
  </action>
  <acceptance_criteria>
    `grep -rn "onSaveHistory\|HistoryEntry\|HistoryStore" Tools/TextDiff/ Tools/Markdown/` returns nothing.
  </acceptance_criteria>
  <done>Text Diff and Markdown tools build with zero history references.</done>
</task>

<task type="auto">
  <name>Task 3: Remove history from Image Compress tool and its test file</name>
  <read_first>
    Tools/ImageCompress/ImageCompressViewModel.swift (onSaveHistory at 133/158; call after successful batch),
    Tools/ImageCompress/ImageCompressView.swift (init(onSaveHistory:) at 19 → VM at 20),
    Tools/ImageCompress/ImageCompressDefinition.swift (ImageCompressViewWrapper at 26 reads @Environment(HistoryStore.self)),
    FlintTests/ImageCompressViewModelTests.swift (11 `ImageCompressViewModel(onSaveHistory:)` call sites; a local `final class HistoryStore` shim at ~267; the "Test 4: History fires exactly once" test at ~256-296)
  </read_first>
  <action>
    In ImageCompressViewModel.swift: delete the `onSaveHistory` stored property (133), init param (158), and the `onSaveHistory(HistoryEntry(...))` call that fires once per successful batch. Keep the off-main batch loop, per-row progress, cancellation, and memory-bound logic unchanged.
    In ImageCompressView.swift: change `init(onSaveHistory:)` (19) to a no-closure init building `ImageCompressViewModel()`.
    In ImageCompressDefinition.swift: delete `ImageCompressViewWrapper` and its `@Environment(HistoryStore.self)`; change `makeView` to `AnyView(ImageCompressView())` directly.
    In ImageCompressViewModelTests.swift: replace every `ImageCompressViewModel(onSaveHistory: { ... })` construction with `ImageCompressViewModel()`. DELETE the entire "Test 4: History fires exactly once per successful batch" test (`testHistoryFiresOnce`, ~256-296) and its local `final class HistoryStore` shim — that behavior no longer exists. Keep all other tests (compression correctness, cancellation, memory) intact.
  </action>
  <acceptance_criteria>
    `grep -rn "onSaveHistory\|HistoryEntry\|HistoryStore" Tools/ImageCompress/ FlintTests/ImageCompressViewModelTests.swift` returns nothing.
    `grep -n "testHistoryFiresOnce" FlintTests/ImageCompressViewModelTests.swift` returns nothing.
  </acceptance_criteria>
  <done>Image Compress tool and its test file build with zero history references; the history-fires-once test is gone.</done>
</task>

</tasks>

<verification>
- `grep -rn "onSaveHistory\|HistoryEntry\|HistoryStore" Tools/UUID/ Tools/Timestamp/ Tools/TextDiff/ Tools/Markdown/ Tools/ImageCompress/ FlintTests/ImageCompressViewModelTests.swift` returns nothing.
- After Waves 1 completes across plans 01/02/03: `grep -rn "onSaveHistory" --include="*.swift" Tools/` returns nothing (all 13 tools clean).
</verification>

<success_criteria>
UUID, Timestamp, Text Diff, Markdown, and Image Compress tools plus ImageCompressViewModelTests have zero references to onSaveHistory, HistoryEntry, or HistoryStore. All non-history tool behavior and non-history tests are unchanged.
</success_criteria>

<output>
Create `.planning/phases/06-remove-the-history-feature/06-03-SUMMARY.md` when done
</output>
