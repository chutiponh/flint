---
phase: 06-remove-the-history-feature
plan: 07
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05, 06-06]
files_modified: []
autonomous: false
requirements: [INFRA-08, INFRA-10, INFRA-17]
must_haves:
  truths:
    - "The app builds clean from a fresh DerivedData with no history/GRDB references"
    - "The full test suite passes"
    - "No History or GRDB symbol remains anywhere outside build/"
    - "Global search still returns tools; no tool is broken by the removal"
  artifacts: []
  key_links:
    - from: "xcodebuild clean build"
      to: "green result"
      via: "no unresolved symbols"
      pattern: "BUILD SUCCEEDED"
---

<objective>
Prove the history removal is complete and non-breaking: a clean build from fresh DerivedData, the full test suite green, a repo-wide grep that no history/GRDB symbol survives outside build/, and a human verification that global search still works and no tool is broken.

This is the phase goal gate. It runs last, after every code and project change (Waves 1-3) has landed.

Purpose: Confirm the phase goal — history gone, search tools-only, build clean, tests green, no dead code/dependency.
Output: A verified history-free build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<memory_note>
Per project memory: "tool missing" or stale-binary surprises in UAT are usually a stale binary from one of ~10 DerivedData folders. This gate MUST clean DerivedData / use a clean build so the tested binary reflects the current source. Open the freshly-built app by its full path from the build output dir.
</memory_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: No-dead-symbols grep gate</name>
  <files>(read-only verification — no files modified unless a stray symbol must be fixed)</files>
  <read_first>
    .planning/phases/06-remove-the-history-feature/06-06-SUMMARY.md (confirm files deleted + GRDB dropped),
    ROADMAP.md Phase 6 "Verify" line
  </read_first>
  <action>
    Run a repo-wide search (excluding build/, dist/, .git/, and .planning/) for any surviving history or GRDB symbol in source. Search for these tokens across `*.swift` and the pbxproj: `HistoryStore`, `HistoryEntry`, `HistoryPanelView`, `HistoryRowView`, `onSaveHistory`, `historyLimit`, `historyResults`, `onSelectHistoryEntry`, `onShowHistory`, `HistoryPreferencesTab`, `toggleHistory`, `GRDB`, `groue/GRDB`. Any match outside build/ is a failure — trace it back to the owning file and fix (a stray comment, a missed call site) before the build gate. Allowed residue: none in source; `.planning/` docs and this SUMMARY may mention the terms historically.
  </action>
  <verify>
    <automated>test -z "$(grep -rn "HistoryStore\|HistoryEntry\|HistoryPanelView\|HistoryRowView\|onSaveHistory\|historyLimit\|historyResults\|onSelectHistoryEntry\|onShowHistory\|HistoryPreferencesTab\|toggleHistory\|GRDB" --include='*.swift' . | grep -v '/build/' | grep -v '/dist/')" && test -z "$(grep -n 'GRDB\|HistoryStore\|HistoryEntry\|HistoryPanelView\|HistoryRowView' Flint.xcodeproj/project.pbxproj)"</automated>
  </verify>
  <acceptance_criteria>
    `grep -rn "HistoryStore\|HistoryEntry\|HistoryPanelView\|HistoryRowView\|onSaveHistory\|historyLimit\|historyResults\|onSelectHistoryEntry\|onShowHistory\|HistoryPreferencesTab\|toggleHistory\|GRDB" --include="*.swift" . | grep -v "/build/" | grep -v "/dist/"` returns nothing.
    `grep -n "GRDB\|HistoryStore\|HistoryEntry\|HistoryPanelView\|HistoryRowView" Flint.xcodeproj/project.pbxproj` returns nothing.
  </acceptance_criteria>
  <done>Zero history/GRDB symbols in any source file or the pbxproj.</done>
</task>

<task type="auto">
  <name>Task 2: Clean build + full test suite</name>
  <files>(read-only verification — may edit a source file only to close a missed-reference build break)</files>
  <read_first>
    scripts/release-free.sh (for the xcodebuild scheme/config invocation used by this project — scheme "Flint"),
    Flint.xcodeproj/xcshareddata/xcschemes/Flint.xcscheme (confirm scheme name "Flint")
  </read_first>
  <action>
    Run a clean build and the full test suite against a fresh derived-data path so no stale binary or stale GRDB artifact leaks in:
    - Clean: `xcodebuild clean -project Flint.xcodeproj -scheme Flint`.
    - Build: `xcodebuild build -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS' -derivedDataPath /tmp/flint-p6-build`.
    - Test: `xcodebuild test -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS' -derivedDataPath /tmp/flint-p6-build`.
    The package graph must resolve WITHOUT GRDB (confirms the drop). The build must emit BUILD SUCCEEDED. Every test must pass — note the surviving test files are all Transformer/Editor/Reorder/PNG/SwiftDiff tests plus the cleaned ImageCompressViewModelTests; HistorySearchTests is gone.
    If the build fails on an unresolved history symbol, that means a Wave 1/2 reference was missed — fix at the source file (this plan may edit source to close such a gap) and re-run.
  </action>
  <verify>
    <automated>xcodebuild clean -project Flint.xcodeproj -scheme Flint >/dev/null 2>&1; xcodebuild build test -project Flint.xcodeproj -scheme Flint -destination 'platform=macOS' -derivedDataPath /tmp/flint-p6-build 2>&1 | tail -5 | grep -Eq "BUILD SUCCEEDED|TEST SUCCEEDED|\*\* TEST SUCCEEDED \*\*"</automated>
  </verify>
  <acceptance_criteria>
    `xcodebuild build ... -derivedDataPath /tmp/flint-p6-build` exits 0 and output contains BUILD SUCCEEDED.
    `xcodebuild test ... -derivedDataPath /tmp/flint-p6-build` exits 0 with all tests passing (TEST SUCCEEDED).
  </acceptance_criteria>
  <done>Clean build succeeds and the full test suite is green with GRDB no longer in the package graph.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify search + tools still work</name>
  <files>(no files modified — human verification of the freshly built app)</files>
  <read_first>
    /tmp/flint-p6-build/Build/Products/Debug/Flint.app (the freshly built binary from Task 2)
  </read_first>
  <action>
    Hand off to the user to verify the freshly built app per the how-to-verify steps below. Do not auto-approve — this checkpoint is blocking.
  </action>
  <what-built>
    History removed end-to-end: no history panel, no per-tool history capture, no history in global search, no history-limit preference, GRDB dropped. The app was clean-built at /tmp/flint-p6-build and the full test suite passed.
  </what-built>
  <how-to-verify>
    1. Launch the freshly built app by full path: `open /tmp/flint-p6-build/Build/Products/Debug/Flint.app` (do NOT open an old copy — stale DerivedData binaries are a known trap).
    2. Open the popover (menubar icon or ⌘⇧Space). Confirm: the launcher shows the tools grid and a welcome/empty affordance, and there is NO recent-history list.
    3. Type a tool name (e.g. "json", "base64") in the search field. Confirm: matching tools appear and ↑/↓ + Enter open a tool. The placeholder reads "Search tools…" (not "…or history").
    4. Type "history" in the search field. Confirm: it does NOT open any history panel — it just filters tools (likely showing none or unrelated matches). No history view appears.
    5. Press ⌘H while the popover is focused. Confirm: nothing history-related happens (the shortcut is gone).
    6. Open Preferences (⌘, or the gear). Confirm: tabs are General / Appearance / Per-Tool only — there is NO History tab and NO history-limit control.
    7. Open two or three tools (e.g. JWT decode a token, Hash some text, Base64 encode) and confirm they still work and nothing is broken by the removal.
  </how-to-verify>
  <verify>
    <human-check>User confirms tools-only launcher, tools-only search, no ⌘H, no History pref tab, and tools still work.</human-check>
  </verify>
  <acceptance_criteria>
    User types "approved" after all seven verification steps pass on the freshly built binary.
  </acceptance_criteria>
  <done>User confirms the history feature is gone and nothing reachable is broken.</done>
  <resume-signal>Type "approved" if all checks pass, or describe what is broken.</resume-signal>
</task>

</tasks>

<verification>
- No-dead-symbols grep returns nothing (source + pbxproj).
- `xcodebuild build` and `xcodebuild test` both succeed on a clean derived-data path.
- Human confirms: tools-only launcher, tools-only search, no ⌘H, no History pref tab, tools still work.
</verification>

<success_criteria>
The phase goal is met: the history feature is fully gone (panel, per-tool capture, search entries, limit pref), global search still works for tools, the app builds clean, the full test suite is green, no dead history/GRDB code or unused dependency remains, and nothing a user could reach is broken.
</success_criteria>

<output>
Create `.planning/phases/06-remove-the-history-feature/06-07-SUMMARY.md` when done
</output>
