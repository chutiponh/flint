---
phase: 06-remove-the-history-feature
plan: 06
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05]
files_modified:
  - Flint.xcodeproj/project.pbxproj
  - Core/Services/HistoryStore.swift
  - Core/Models/HistoryEntry.swift
  - UI/HistoryPanelView.swift
  - UI/Components/HistoryRowView.swift
  - FlintTests/HistorySearchTests.swift
autonomous: true
requirements: [INFRA-08]
must_haves:
  truths:
    - "The 5 history source files no longer exist on disk"
    - "The pbxproj contains no references to the 5 deleted files or to the GRDB package"
    - "GRDB is no longer a dependency of either target"
  artifacts:
    - path: "Flint.xcodeproj/project.pbxproj"
      provides: "Project file with no history file refs and no GRDB package/product/build entries"
  key_links:
    - from: "Flint.xcodeproj/project.pbxproj"
      to: "source files on disk"
      via: "PBXFileReference paths all resolve to existing files"
      pattern: "sourcecode.swift"
---

<objective>
Delete the five now-orphaned history files, remove every reference to them from the Xcode project, and drop the GRDB Swift package entirely — after Waves 1 and 2 the only real `import GRDB` sites are HistoryStore.swift and HistoryEntry.swift (both deleted here), so nothing else needs GRDB.

This is the wave where the tree transitions to fully history-free. It MUST run after 06-01..06-05: those plans removed every reference to HistoryStore / HistoryEntry / HistoryPanelView / HistoryRowView. Deleting the files before those references are gone would break the build.

Purpose: Remove all dead history code and the unused GRDB dependency.
Output: Five files gone, pbxproj clean, GRDB dropped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

<pbxproj_map>
Xcode 14-format pbxproj with stable synthetic IDs. Entries to remove:

Five history files — each has THREE entry types:
- PBXBuildFile (lines ~53,54,115,116,120): `... /* X.swift in Sources */ = {isa = PBXBuildFile; fileRef = ...};`
- PBXFileReference (lines ~157,158,217,218,222): `... /* X.swift */ = {isa = PBXFileReference; ... path = X.swift; ...};`
- PBXGroup children (lines ~343,351,558,579,611): `... /* X.swift */,` inside a group's `children` list
- Sources build phase (lines ~767,768,813,814,871): `... /* X.swift in Sources */,` inside a `files` list
  (HistorySearchTests is in the FlintTests Sources phase; the other four are in the Flint app Sources phase)

GRDB package — SEVEN entry types:
- 2 PBXBuildFile "GRDB in Frameworks" (lines ~88,93; the second is "(Tests)")
- 2 Frameworks build phase entries referencing those (lines ~279,294)
- packageProductDependencies: `001300000000001 /* GRDB */,` in the Flint target (line ~659) and `001300000000011 /* GRDB */,` in the FlintTests target (line ~686)
- packageReferences: `001B00000000001 /* XCRemoteSwiftPackageReference "GRDB.swift" */,` (line ~721)
- XCRemoteSwiftPackageReference block for "GRDB.swift" (lines ~1145-1149)
- 2 XCSwiftPackageProductDependency blocks `001300000000001 /* GRDB */` and `001300000000011 /* GRDB */` (lines ~1212-1216, ~1232-1236)
</pbxproj_map>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the five history source files</name>
  <read_first>
    Core/Services/HistoryStore.swift, Core/Models/HistoryEntry.swift, UI/HistoryPanelView.swift, UI/Components/HistoryRowView.swift, FlintTests/HistorySearchTests.swift (confirm each is history-only and orphaned — no non-history exports)
  </read_first>
  <action>
    Delete these five files from disk using `git rm`:
    - Core/Services/HistoryStore.swift
    - Core/Models/HistoryEntry.swift
    - UI/HistoryPanelView.swift
    - UI/Components/HistoryRowView.swift
    - FlintTests/HistorySearchTests.swift
    (Use `git rm <path>` for each so the deletion is staged. These are entirely history-specific — the Wave 1/2 plans removed every import.)
  </action>
  <acceptance_criteria>
    `ls Core/Services/HistoryStore.swift Core/Models/HistoryEntry.swift UI/HistoryPanelView.swift UI/Components/HistoryRowView.swift FlintTests/HistorySearchTests.swift 2>&1 | grep -c "No such file"` returns 5.
  </acceptance_criteria>
  <done>All five history files are deleted from disk.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the five files from the pbxproj</name>
  <read_first>
    Flint.xcodeproj/project.pbxproj (grep the five basenames first to get current line numbers, then edit; see &lt;pbxproj_map&gt; for the four entry types per file)
  </read_first>
  <action>
    In Flint.xcodeproj/project.pbxproj, delete ALL lines that reference the five deleted files. For EACH of HistoryEntry.swift, HistoryStore.swift, HistoryPanelView.swift, HistoryRowView.swift, HistorySearchTests.swift remove its four entry types:
    1. the PBXBuildFile line (`... in Sources */ = {isa = PBXBuildFile; ...};`),
    2. the PBXFileReference line (`... */ = {isa = PBXFileReference; ... path = X.swift; ...};`),
    3. the PBXGroup `children` entry line (`... /* X.swift */,`),
    4. the Sources build-phase `files` entry line (`... /* X.swift in Sources */,`).
    Work by grepping each basename (`grep -n "HistoryStore.swift" ...`) so you catch exactly the lines that mention it. Do not remove entries for any other file. Preserve all surrounding structure and commas/formatting of neighboring lines.
  </action>
  <acceptance_criteria>
    `grep -c "HistoryStore.swift\|HistoryEntry.swift\|HistoryPanelView.swift\|HistoryRowView.swift\|HistorySearchTests.swift" Flint.xcodeproj/project.pbxproj` returns 0.
  </acceptance_criteria>
  <done>No reference to any deleted history file remains in the pbxproj.</done>
</task>

<task type="auto">
  <name>Task 3: Drop the GRDB package from the pbxproj</name>
  <read_first>
    Flint.xcodeproj/project.pbxproj (grep "GRDB" and "groue" for current line numbers; see &lt;pbxproj_map&gt; for the seven entry types),
    Tools/Color/ColorViewModel.swift + Tools/NumberBase/NumberBaseViewModel.swift (confirm no `import GRDB` after Wave 1 — should already be gone)
  </read_first>
  <action>
    First confirm no source file imports GRDB: `grep -rln "^import GRDB" --include="*.swift" . | grep -v build` must return nothing (HistoryStore/HistoryEntry are deleted; Color/NumberBase never had a real import). If any real `import GRDB` remains, STOP and report it — do not drop the package while a consumer exists.
    Then in Flint.xcodeproj/project.pbxproj remove every GRDB entry (see the seven types in &lt;pbxproj_map&gt;):
    - both `GRDB in Frameworks` PBXBuildFile lines (app + tests),
    - both Frameworks build-phase `files` entries that reference them,
    - the `001300000000001 /* GRDB */,` line in the Flint target's `packageProductDependencies`,
    - the `001300000000011 /* GRDB */,` line in the FlintTests target's `packageProductDependencies`,
    - the `001B00000000001 /* XCRemoteSwiftPackageReference "GRDB.swift" */,` line in the project `packageReferences`,
    - the entire `001B00000000001 /* XCRemoteSwiftPackageReference "GRDB.swift" */ = { ... };` block (repositoryURL groue/GRDB.swift),
    - both `XCSwiftPackageProductDependency` blocks for GRDB (`001300000000001` and `001300000000011`).
    Do not touch any other package (KeyboardShortcuts, MenuBarExtraAccess, HighlightSwift, UUIDv7, ChromaKit, swift-markdown, Sparkle).
  </action>
  <acceptance_criteria>
    `grep -c "GRDB\|groue/GRDB" Flint.xcodeproj/project.pbxproj` returns 0.
    `grep -rln "^import GRDB" --include="*.swift" . | grep -v build` returns nothing.
    The other seven package references still present: `grep -c "KeyboardShortcuts\|MenuBarExtraAccess\|HighlightSwift\|UUIDv7\|ChromaKit\|swift-markdown\|Sparkle" Flint.xcodeproj/project.pbxproj` is > 0.
  </acceptance_criteria>
  <done>GRDB is fully removed from the pbxproj; no source imports GRDB; all other packages intact.</done>
</task>

</tasks>

<verification>
- Five history files gone from disk and from the pbxproj.
- No `GRDB` / `groue/GRDB` token anywhere in the pbxproj.
- No `import GRDB` in any non-build .swift file.
- (Actual `xcodebuild` clean build + test is the Wave-4 gate; this plan makes the tree buildable by removing the last dangling references. Package-graph resolution without GRDB is validated there.)
</verification>

<success_criteria>
The five history files are deleted, the pbxproj has zero references to them, and the GRDB package is fully dropped with no remaining source consumer.
</success_criteria>

<output>
Create `.planning/phases/06-remove-the-history-feature/06-06-SUMMARY.md` when done
</output>
