---
phase: 06-remove-the-history-feature
plan: 05
type: execute
wave: 2
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - App/FlintApp.swift
  - UI/MenuBarPopoverView.swift
  - UI/MainWindowView.swift
  - Core/Services/PreferencesStore.swift
  - UI/PreferencesView.swift
autonomous: true
requirements: [INFRA-08, INFRA-12, INFRA-13, INFRA-16]
must_haves:
  truths:
    - "The popover no longer shows recent history, a history nav view, or a ⌘H toggle-history shortcut"
    - "The 'history' search query no longer opens a history panel"
    - "Preferences no longer has a History tab or a history-limit control"
    - "No HistoryStore is injected into any scene; historyLimit is gone from PreferencesStore"
    - "App still launches, popover opens, tools open, preferences open (General/Appearance/Per-Tool tabs)"
  artifacts:
    - path: "App/FlintApp.swift"
      provides: "App scenes with no HistoryStore @State or .environment injection, no historyLimit onChange"
    - path: "Core/Services/PreferencesStore.swift"
      provides: "Preferences store with no historyLimit property and no Keys.historyLimit"
    - path: "UI/MenuBarPopoverView.swift"
      provides: "Popover with no history nav state, no recent-history view, no ⌘H shortcut"
  key_links:
    - from: "App/FlintApp.swift"
      to: "MenuBarPopoverView"
      via: "environment injections without historyStore"
      pattern: "\\.environment\\("
---

<objective>
Remove all app-level history wiring, the history navigation and ⌘H shortcut in the popover, the unused HistoryStore reference in the workspace window, and the entire History preference (limit property, key, and Preferences tab).

Purpose: The app shell, popover, workspace, and preferences stop referencing HistoryStore / historyLimit / the history panel so the store, model, panel, and row views can be deleted in Wave 3.
Output: An app that builds and runs — popover, tools, and preferences all functional — with no history surface reachable by the user.

This plan owns the caller side of the search change: MenuBarPopoverView drives its own history list/nav (independent of SearchView's params), so its history removal lives here, not in 06-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip history from FlintApp and MainWindowView</name>
  <read_first>
    App/FlintApp.swift (full file — `@State private var historyStore = HistoryStore()` at 24; `.environment(historyStore)` at 43 and 72; the `.onChange(of: prefs.historyLimit, initial: true)` block at 52-55; the `.environment(historyStore)` on the Settings scene at 102 with its "CR-01: needed by HistoryPreferencesTab" comment),
    UI/MainWindowView.swift (`@Environment(HistoryStore.self) private var historyStore` at 11 — declared but never used)
  </read_first>
  <action>
    In FlintApp.swift:
    - Delete `@State private var historyStore = HistoryStore()` (line 24).
    - Delete every `.environment(historyStore)` injection: on the MenuBarPopoverView (43), on MainWindowView (72), and on the Settings/PreferencesView scene (102) — remove the trailing "CR-01" comment on that line too.
    - Delete the entire `.onChange(of: prefs.historyLimit, initial: true) { _, newLimit in historyStore.historyLimit = newLimit }` modifier (52-55) including its "WR-04" comment.
    In MainWindowView.swift:
    - Delete the `@Environment(HistoryStore.self) private var historyStore` line (11). It is unused; nothing else in the file references it.
  </action>
  <acceptance_criteria>
    `grep -n "HistoryStore\|historyStore\|historyLimit" App/FlintApp.swift UI/MainWindowView.swift` returns nothing.
  </acceptance_criteria>
  <done>FlintApp injects no HistoryStore and has no historyLimit sync; MainWindowView has no HistoryStore env.</done>
</task>

<task type="auto">
  <name>Task 2: Strip history navigation, recent-history, and ⌘H from MenuBarPopoverView</name>
  <read_first>
    UI/MenuBarPopoverView.swift (full file — @Environment(HistoryStore.self) at 55; PopoverNavigationState `.history` case at 51; header comment ⌘H at 6-7; the ⌘H "Toggle History" button at 222-226; toggleHistory() at 360-367; the `"history"` query branch in .onChange(of: searchText) at 175-177; bodyContent `.searchResults` history list at 474/481-484 and `.history` case at 488-494; recentHistoryView at 514-550 used by `.root`; filteredHistoryList at 552-570; the search TextField placeholder/accessibility "Search tools or history" at 398/402; trailing comment "HistoryRowView is defined in..." at 656)
  </read_first>
  <action>
    In MenuBarPopoverView.swift:
    - Remove `@Environment(HistoryStore.self) private var historyStore` (55).
    - In `enum PopoverNavigationState`, delete the `case history` (51). Update its doc comments (the `.root` case comment mentions "recent history").
    - Delete the ⌘H "Toggle History" hidden Button (222-226) and the `toggleHistory()` function (360-367). Update the header doc block (6-7) and the inline "⌘H — toggle history" comments (207) to remove the ⌘H line. This retires the INFRA-16 toggle-history binding.
    - In `.onChange(of: searchText)` (169-181), delete the `else if newValue... == "history"` branch that sets `navigationState = .history`; keep the empty-string and `.searchResults` branches.
    - In `bodyContent`: in the `.root` case, remove the `Divider()` + `recentHistoryView` so the launcher shows only `AllToolsGridView` (keep the grid). In the `.searchResults` case, remove the `historyStore.search(query)` call, the `historyMatches`/`filteredHistoryList` usage, and the associated Divider — leave the filtered `AllToolsGridView` + `Spacer`. Delete the entire `.history` case.
    - Delete the `recentHistoryView` computed property (514-550) and the `filteredHistoryList(_:)` function (552-570). If removing `recentHistoryView` also removes the only "Welcome to Flint" empty-state, preserve that empty state by moving it (or an equivalent) so the launcher still shows a welcome message when the grid is the sole content — OR, if AllToolsGridView already renders acceptably on its own, drop the welcome view. Prefer keeping a simple welcome/empty affordance; do not leave the launcher visually broken.
    - Change the search TextField placeholder (398) from "Search tools or history…" to "Search tools…" and its `.accessibilityLabel` (402) from "Search tools or history" to "Search tools".
    - Delete the trailing `// HistoryRowView is defined in ...` comment (656).
    The file must not reference HistoryStore, HistoryEntry, HistoryRowView, HistoryPanelView, or the `.history` nav state.
  </action>
  <acceptance_criteria>
    `grep -n "HistoryStore\|HistoryEntry\|HistoryRowView\|HistoryPanelView\|\.history\b\|toggleHistory\|Toggle History\|or history" UI/MenuBarPopoverView.swift` returns nothing.
    `grep -n "keyboardShortcut(\"h\"" UI/MenuBarPopoverView.swift` returns nothing (⌘H binding removed).
  </acceptance_criteria>
  <done>Popover has no history nav, no recent/filtered history lists, no ⌘H shortcut, tools-only search placeholder; launcher still shows the tools grid and a welcome/empty affordance.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the History preference (limit + tab)</name>
  <read_first>
    Core/Services/PreferencesStore.swift (`historyLimit` property at 178-184; `Keys.historyLimit` at 246),
    UI/PreferencesView.swift (@Environment(HistoryStore.self) at 14; TabView with HistoryPreferencesTab at 28-31 + `.environment(historyStore)` at 40; `HistoryPreferencesTab` struct at 245-317 which reads historyStore, prefs.historyLimit, historyStore.clearUnpinned(); file header at 2 lists "History" tab)
  </read_first>
  <action>
    In PreferencesStore.swift:
    - Delete the `// MARK: - History Limit (INFRA-13)` section and the `var historyLimit: Int { ... }` computed property (175-184).
    - Delete `static let historyLimit = "lathe.historyLimit"` from the `Keys` enum (246).
    In PreferencesView.swift:
    - Delete `@Environment(HistoryStore.self) private var historyStore` (14) and the `.environment(historyStore)` propagation on the TabView (40).
    - Remove the `HistoryPreferencesTab()` entry and its `.tabItem { Label("History", ...) }` from the `TabView` (28-31), leaving General, Appearance, Per-Tool tabs.
    - Delete the entire `private struct HistoryPreferencesTab: View { ... }` (245-317) — it reads historyStore.clearUnpinned() and prefs.historyLimit, all being removed.
    - Update the file header comment (2) to drop "History" from the tab list.
    The file must not reference HistoryStore, historyLimit, or HistoryPreferencesTab.
  </action>
  <acceptance_criteria>
    `grep -n "historyLimit\|Keys.historyLimit" Core/Services/PreferencesStore.swift` returns nothing.
    `grep -n "HistoryStore\|historyLimit\|HistoryPreferencesTab\|clearUnpinned" UI/PreferencesView.swift` returns nothing.
    `grep -n "GeneralPreferencesTab\|AppearancePreferencesTab\|PerToolPreferencesTab" UI/PreferencesView.swift` still matches (other tabs intact).
  </acceptance_criteria>
  <done>PreferencesStore has no historyLimit; Preferences shows General/Appearance/Per-Tool tabs with no History tab and no history-limit control.</done>
</task>

</tasks>

<verification>
- `grep -rn "HistoryStore\|HistoryEntry\|historyLimit\|HistoryPreferencesTab\|toggleHistory\|\.history\b" App/FlintApp.swift UI/MenuBarPopoverView.swift UI/MainWindowView.swift Core/Services/PreferencesStore.swift UI/PreferencesView.swift` returns nothing.
- No `keyboardShortcut("h", ...)` remains in the popover.
</verification>

<success_criteria>
No HistoryStore is injected anywhere; the popover has no history UI or ⌘H shortcut; the "history" search query no longer opens a panel; Preferences has no History tab or history-limit control; PreferencesStore has no historyLimit. App shell, popover, workspace, and preferences remain functional.
</success_criteria>

<output>
Create `.planning/phases/06-remove-the-history-feature/06-05-SUMMARY.md` when done
</output>
