---
phase: 06-remove-the-history-feature
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - Core/Services/SearchResultsMerger.swift
  - UI/SearchView.swift
autonomous: true
requirements: [INFRA-10]
must_haves:
  truths:
    - "Global fuzzy search returns tools only — no history rows, no 'Show full history' affordance"
    - "Search still ranks and lists matching tools, keyboard-navigable (↑↓ Enter)"
    - "SearchResultsMerger is a pure tools-only merge with no HistoryEntry reference"
  artifacts:
    - path: "Core/Services/SearchResultsMerger.swift"
      provides: "Tools-only merge: SearchResult has only .tool, MergedSearchResults has only toolResults"
      contains: "toolResults"
    - path: "UI/SearchView.swift"
      provides: "Search view with no HistoryStore, no history section, tools-only empty copy"
  key_links:
    - from: "UI/SearchView.swift"
      to: "SearchResultsMerger.merge"
      via: "merge(tools:query:) call with no history arg"
      pattern: "SearchResultsMerger\\.merge"
---

<objective>
Make global fuzzy search tools-only while keeping it fully working (INFRA-10). Strip the history half of both the pure merge function and the search view: drop the `.historyEntry` case, `historyResults`, `onSelectHistoryEntry`, `onShowHistory`, the History section, the "Show full history…" button, and the `@Environment(HistoryStore.self)`. Update the empty-state copy from "No tools or history" to "No tools".

Purpose: Search survives the history removal as a tools-only feature; SearchResultsMerger and SearchView stop referencing history so the store can be deleted in Wave 3.
Output: A tools-only search path that still ranks tools and supports ↑↓/Enter navigation.

NOTE: SearchView is instantiated by MenuBarPopoverView, but the surface-area map shows MenuBarPopoverView routes search through its OWN AllToolsGridView filter and history list (not through SearchView's `onSelectHistoryEntry`/`onShowHistory` params). The caller-side edits for MenuBarPopoverView live in plan 06-05. This plan only edits SearchView + SearchResultsMerger; do NOT edit MenuBarPopoverView here (file-ownership boundary for parallel execution).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
Current SearchResultsMerger public shape (to be reduced to tools-only):
```swift
enum SearchResult: Sendable { case tool(ToolDefinition); case historyEntry(HistoryEntry) }
struct MergedSearchResults: Sendable {
    let toolResults: [ToolDefinition]
    let historyResults: [HistoryEntry]
    var isEmpty: Bool { toolResults.isEmpty && historyResults.isEmpty }
}
static func merge(tools:history:query:) -> MergedSearchResults
static func isHistoryQuery(_:) -> Bool           // remove
static func defaultState(allTools:recentHistory:) -> MergedSearchResults
```
Target tools-only shape:
```swift
enum SearchResult: Sendable { case tool(ToolDefinition) }
struct MergedSearchResults: Sendable {
    let toolResults: [ToolDefinition]
    var isEmpty: Bool { toolResults.isEmpty }
}
static func merge(tools:query:) -> MergedSearchResults
static func defaultState(allTools:) -> MergedSearchResults
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce SearchResultsMerger to tools-only</name>
  <read_first>
    Core/Services/SearchResultsMerger.swift (full file — SearchResult enum, MergedSearchResults struct, merge/isHistoryQuery/defaultState functions)
  </read_first>
  <action>
    In SearchResultsMerger.swift:
    - Remove the `.historyEntry(HistoryEntry)` case from `SearchResult` (leaving only `.tool`).
    - Remove `historyResults` from `MergedSearchResults`; simplify `isEmpty` to `toolResults.isEmpty`.
    - Change `merge(tools:history:query:)` to `merge(tools:query:)` — drop the `history` param, the `rankedHistory` sort, and the `historyResults:` in the return. Keep the tool ranking exactly as-is.
    - Delete `isHistoryQuery(_:)` entirely (the "history" query trigger is a history-panel affordance being removed).
    - Change `defaultState(allTools:recentHistory:)` to `defaultState(allTools:)` returning `MergedSearchResults(toolResults: allTools)`.
    - Update the top-of-file doc comment: it currently says "Combines ToolRegistry tool matches with HistoryStore history matches" — change to tools-only.
    The file must not import or reference HistoryEntry.
  </action>
  <acceptance_criteria>
    `grep -n "HistoryEntry\|historyResults\|historyEntry\|isHistoryQuery\|recentHistory" Core/Services/SearchResultsMerger.swift` returns nothing.
    `grep -n "toolResults" Core/Services/SearchResultsMerger.swift` still matches (tool merging preserved).
  </acceptance_criteria>
  <done>SearchResultsMerger is a pure tools-only merge with no history references and no unused history function.</done>
</task>

<task type="auto">
  <name>Task 2: Make SearchView tools-only</name>
  <read_first>
    UI/SearchView.swift (full file — @Environment(HistoryStore.self) at 12; onSelectHistoryEntry/onShowHistory params at 17/18; merged/flatResults at 24-36; empty copy at 84-85; History Section at 114-130; "Show full history" button at 132-146; activateSelected at 153-162),
    Core/Services/SearchResultsMerger.swift as the just-reduced merge API (read after Task 1)
  </read_first>
  <action>
    In SearchView.swift:
    - Delete `@Environment(HistoryStore.self) private var historyStore`.
    - Delete the `onSelectHistoryEntry: (HistoryEntry) -> Void` and `onShowHistory: () -> Void` stored closures.
    - Update `merged` to call `SearchResultsMerger.merge(tools: toolRegistry.search(query), query: query)` (no history arg).
    - Update `flatResults` to be just `merged.toolResults.map { SearchResult.tool($0) }`.
    - Delete the entire History `Section("History")` block (114-130) and the "Show full history…" `Button(action: onShowHistory)` block (132-146).
    - In `activateSelected()` remove the `.historyEntry` switch case (only `.tool` remains — a plain if/for-case or a single-case switch is fine; ensure it compiles with the reduced enum).
    - Change the empty-state text from `"No tools or history matching \"\(query)\""` to `"No tools matching \"\(query)\""`. Update the adjacent `// UI-SPEC Copywriting` comment to match.
    - The file must not reference HistoryStore, HistoryEntry, HistoryRowView, onSelectHistoryEntry, or onShowHistory.
    NOTE: If SearchView is not currently instantiated anywhere (MenuBarPopoverView uses its own grid), it still must compile as a self-consistent tools-only view. Do not delete the file.
  </action>
  <acceptance_criteria>
    `grep -n "HistoryStore\|HistoryEntry\|HistoryRowView\|onSelectHistoryEntry\|onShowHistory\|or history" UI/SearchView.swift` returns nothing.
    `grep -n "SearchResultsMerger.merge(tools:" UI/SearchView.swift` matches with no `history:` label present.
  </acceptance_criteria>
  <done>SearchView renders tools-only, keyboard-navigable, with tools-only empty copy and no history references.</done>
</task>

</tasks>

<verification>
- `grep -rn "HistoryEntry\|HistoryStore\|historyResults\|onSelectHistoryEntry\|onShowHistory\|isHistoryQuery" Core/Services/SearchResultsMerger.swift UI/SearchView.swift` returns nothing.
- Tool ranking logic in SearchResultsMerger.merge is preserved (name-exact > prefix > alpha).
- (Build/behavior gate is in the final wave. Note: HistorySearchTests.swift — the only test of SearchResultsMerger — is deleted in Wave 3, so no test references the old merge signature after that.)
</verification>

<success_criteria>
Global fuzzy search is tools-only: SearchResultsMerger and SearchView have zero history references, tool ranking and ↑↓/Enter navigation intact, empty copy reads "No tools matching…".
</success_criteria>

<output>
Create `.planning/phases/06-remove-the-history-feature/06-04-SUMMARY.md` when done
</output>
