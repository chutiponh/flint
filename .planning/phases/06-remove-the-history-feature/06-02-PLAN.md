---
phase: 06-remove-the-history-feature
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Tools/Color/ColorViewModel.swift
  - Tools/Color/ColorView.swift
  - Tools/NumberBase/NumberBaseViewModel.swift
  - Tools/NumberBase/NumberBaseView.swift
  - Tools/NumberBase/NumberBaseDefinition.swift
  - Tools/Regex/RegexViewModel.swift
  - Tools/Regex/RegexView.swift
  - Tools/JSONFormatter/JSONFormatterViewModel.swift
  - Tools/JSONFormatter/JSONFormatterView.swift
autonomous: true
requirements: [INFRA-08]
must_haves:
  truths:
    - "Color, Number Base, Regex, and JSON tools compile and work with no history capture"
    - "No `import GRDB` (real or commented) remains in Color or NumberBase ViewModels"
  artifacts:
    - path: "Tools/Color/ColorViewModel.swift"
      provides: "Color logic with no onSaveHistory and no GRDB comment"
    - path: "Tools/NumberBase/NumberBaseDefinition.swift"
      provides: "NumberBase Definition with no HistoryStore wrapper"
    - path: "Tools/JSONFormatter/JSONFormatterView.swift"
      provides: "JSON view with no @Environment(HistoryStore)"
  key_links:
    - from: "Tools/NumberBase/NumberBaseDefinition.swift"
      to: "NumberBaseView()"
      via: "makeView builds view with no closure"
      pattern: "NumberBaseView\\(\\)"
---

<objective>
Strip all per-tool history capture from the Color, Number Base, Regex, and JSON Formatter tools. Remove each ViewModel's `onSaveHistory: (HistoryEntry) -> Void` param, stored property, and call sites; remove the wiring that feeds it; and delete the stale `// ... never import GRDB here` comments in ColorViewModel and NumberBaseViewModel (these are the two ViewModels the code map flagged for GRDB — the imports are comment-only, no real GRDB code, so nothing GRDB-related survives here).

Purpose: These four tools stop referencing the history subsystem so it can be deleted in Wave 3.
Output: Four tools that build and function with zero history references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

<pattern_note>
Mixed patterns in this plan:
- NumberBase: Wrapper-pattern — NumberBaseDefinition.swift has a wrapper reading @Environment(HistoryStore.self); NumberBaseView has `init(onSaveHistory:)`.
- Color, Regex, JSON: Environment-in-View — the View reads @Environment(HistoryStore.self) and builds the ViewModel with `onSaveHistory: { [historyStore] entry in historyStore.save(entry) }`; the Definition calls `AnyView(SomeView())` with no closure.
IMPORTANT (GRDB): ColorViewModel.swift line 5 and NumberBaseViewModel.swift line 5 contain only a COMMENT mentioning GRDB ("never import GRDB here"). There is no `import GRDB` statement. Delete those comment lines; there is no import to remove.
</pattern_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove history from Color and Regex tools (Environment-in-View pattern)</name>
  <read_first>
    Tools/Color/ColorViewModel.swift (GRDB comment at 5; onSaveHistory at 130/134, call at 274),
    Tools/Color/ColorView.swift (@Environment(HistoryStore.self) at 15; VM build with onSaveHistory at ~31),
    Tools/Regex/RegexViewModel.swift (onSaveHistory at 60/70, call at 223),
    Tools/Regex/RegexView.swift (@Environment at 14; VM build at ~29; preview .environment(HistoryStore()) at 563)
  </read_first>
  <action>
    For BOTH Color and Regex:
    In each ViewModel: delete the `onSaveHistory` stored property, the init param, and the `onSaveHistory(HistoryEntry(...))` call. In ColorViewModel.swift ALSO delete the stale GRDB comment line (line ~5) that says the ViewModel never imports GRDB.
    In each View: delete `@Environment(HistoryStore.self) private var historyStore`; drop the `onSaveHistory: { ... }` argument at the ViewModel construction so it becomes a no-closure init; delete any `.environment(HistoryStore())` in the `#Preview` (RegexView line ~563; ColorView has none per map — verify).
    Keep all color derivation (all-format output, gamut, D-08 row copy) and regex evaluation logic unchanged.
  </action>
  <acceptance_criteria>
    `grep -rn "onSaveHistory\|HistoryEntry\|HistoryStore\|GRDB" Tools/Color/ Tools/Regex/` returns nothing.
  </acceptance_criteria>
  <done>Color and Regex tools build with zero history/GRDB references.</done>
</task>

<task type="auto">
  <name>Task 2: Remove history from JSON Formatter tool (Environment-in-View pattern)</name>
  <read_first>
    Tools/JSONFormatter/JSONFormatterViewModel.swift (onSaveHistory at 57/62, call at 110),
    Tools/JSONFormatter/JSONFormatterView.swift (@Environment(HistoryStore.self) at 9; VM build at ~25; preview .environment(HistoryStore()) at 152)
  </read_first>
  <action>
    In JSONFormatterViewModel.swift: delete the `onSaveHistory` stored property, the init param, and the `onSaveHistory(HistoryEntry(...))` call at ~110. Keep pretty-print/minify/sort/validate logic unchanged.
    In JSONFormatterView.swift: delete `@Environment(HistoryStore.self) private var historyStore`; drop the `onSaveHistory: { [historyStore] entry in ... }` argument so the ViewModel is built with no closure; delete the `.environment(HistoryStore())` line in the `#Preview` at ~152.
  </action>
  <acceptance_criteria>
    `grep -rn "onSaveHistory\|HistoryEntry\|HistoryStore" Tools/JSONFormatter/` returns nothing.
  </acceptance_criteria>
  <done>JSON Formatter tool builds with zero history references.</done>
</task>

<task type="auto">
  <name>Task 3: Remove history from Number Base tool (Wrapper pattern)</name>
  <read_first>
    Tools/NumberBase/NumberBaseViewModel.swift (GRDB comment at 5; onSaveHistory at 58/62, call at 121),
    Tools/NumberBase/NumberBaseView.swift (init(onSaveHistory:) at 15 forwards to VM at 16),
    Tools/NumberBase/NumberBaseDefinition.swift (wrapper reads @Environment(HistoryStore.self), passes closure),
    Tools/Hash/HashDefinition.swift as the cleaned Wrapper-pattern reference (read the 06-01 result)
  </read_first>
  <action>
    In NumberBaseViewModel.swift: delete the `onSaveHistory` stored property, init param, and the `onSaveHistory(HistoryEntry(...))` call at ~121. Delete the stale GRDB comment line at ~5. Keep base conversion / bit-field / two's-complement logic unchanged.
    In NumberBaseView.swift: change `init(onSaveHistory:)` (line 15) to a no-closure init (build `NumberBaseViewModel()`), or remove the custom init and use a State default.
    In NumberBaseDefinition.swift: delete the ViewWrapper struct and its `@Environment(HistoryStore.self)`; change `makeView` to `AnyView(NumberBaseView())` directly.
  </action>
  <acceptance_criteria>
    `grep -rn "onSaveHistory\|HistoryEntry\|HistoryStore\|GRDB" Tools/NumberBase/` returns nothing.
    `grep -n "NumberBaseView()" Tools/NumberBase/NumberBaseDefinition.swift` matches.
  </acceptance_criteria>
  <done>Number Base tool builds with zero history/GRDB references; Definition builds NumberBaseView() directly.</done>
</task>

</tasks>

<verification>
- `grep -rn "onSaveHistory\|HistoryEntry\|HistoryStore\|GRDB" Tools/Color/ Tools/NumberBase/ Tools/Regex/ Tools/JSONFormatter/` returns nothing.
</verification>

<success_criteria>
Color, Number Base, Regex, and JSON tools have zero references to onSaveHistory, HistoryEntry, HistoryStore, or GRDB. All transform behavior unchanged.
</success_criteria>

<output>
Create `.planning/phases/06-remove-the-history-feature/06-02-SUMMARY.md` when done
</output>
